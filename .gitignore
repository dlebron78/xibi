# Secrets and credentials — NEVER commit these
secrets.env
*.env
!.env.example

# Runtime config with real API keys
# (commit config.example.json and profile.example.json as templates instead)
config.json
profile.json

# Deployment reference — contains SSH paths, IPs, usernames
DEPLOY.md

# Database files — personal data, traces, beliefs
*.db
*.db-wal
*.db-shm
*.db-journal
data/

# Conversation logs and workspace mirrors — contain real email content, PII
mirror/

# Agent workflow files — contain deployment paths with usernames
.agent/
.agents/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test / coverage
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/

# Logs
*.log

# NucBox deployment (local paths, not in repo)
bregger_deployment/
bregger_remote/

# Pipeline hold file
tasks/HOLD
deploy.sh
RESTART_BOT.sh

# SSH deploy keys — private key stays local
.ssh/
