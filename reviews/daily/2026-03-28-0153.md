# Pipeline Review — 2026-03-28 05:50

## Action taken

**Waited.** Step-17 ("Critical Bug Fixes") was triggered only ~20 minutes ago
(01:30 -0400 = 05:30 UTC). No open PRs. Jules is actively working — no action taken.

## Pipeline state

- Last merged: PR #18 — Session Context Phase 2: Entity Extraction (steps 15+16), merged 2026-03-28T05:10Z
- CI on main: ✅ passing (most recent run: 2026-03-28T05:40Z — success)
- In flight: step-17 "Critical Bug Fixes" — triggered 05:30 UTC (~20 min ago), no branch yet
- Open PRs: none
- Queued (tasks/pending/): step-18, step-19, step-20
- Tasks/done/: steps 01, 01b, 02–16 (16 steps complete)

## Notes

### Step-17 trigger is fresh — wait is correct

Step-17 was triggered at 01:30 -0400 (05:30 UTC). This run is only ~20 minutes after trigger.
Jules typically delivers within 2–4 hours of trigger. No action until a PR appears.
Next review should check for an open PR around 09:00–11:00 UTC.

### CLI test run (2026-03-28-0139) surfaced critical integration failures

The NucBox CLI test runner completed a full 0/18 run. Root causes (from `reviews/test-runs/cli-test-2026-03-28-0139.md`):

**Infrastructure bugs — NOT addressed by step-17 spec:**

1. **Tool routing broken for small models** — phi4-mini calls `tool="email"` (skill name)
   instead of `tool="list_emails"` (actual function). Executor returns `Unknown tool: email`.
   Fix: flatten tool list in system prompt or add skill-name-to-tool shim in executor.
   → This should be a new BACKLOG item / future step spec.

2. **CLI missing DB migration on startup** — `cli.py` creates `SessionContext` but never
   calls `SchemaManager.migrate()`. Tables (`session_turns`, `session_entities`) don't exist
   on first run. Fix: add `SchemaManager(db_path).migrate()` to `cli.py` startup.
   → Step-17 spec mentions `cli.py` only implicitly via Telegram refactor — this bug
   may be partially addressed if Jules initializes DB in telegram/cli changes, but
   likely needs an explicit fix.

3. **`format="json"` placed inside `options` dict** — `router.py:generate_structured`
   sends the format key where Ollama ignores it (must be top-level payload). Entity
   extraction JSON parse errors result.
   → Not in step-17 spec. New bug to queue.

4. **Test runner I/O capture race** — turn N+1 picks up turn N's leftover output.
   → Test infrastructure only; Jules doesn't need to fix this.

**Partially addressed by step-17 spec:**

5. **Telegram `process_query()` fallback** — PR #18 already moved `_process_message`
   to call `react.run()` directly. Step-17 completes the fix (removes `self.core` entirely,
   removes process_query fallback). Should fix related test failures.

6. **Error detection too narrow in react.py** — step-17 spec specifically addresses this;
   once fixed, test output will include richer error detail in traces.

### Post-step-17 action: queue infrastructure fixes

After step-17 merges, recommend queuing an infrastructure bugfix step that covers:
- Tool routing fix (skill name → tool name resolution)
- CLI startup DB migration
- `format="json"` top-level placement in generate_structured()
- Markdown fence stripping in generate_structured()

These are blocking meaningful CLI pressure test results.

### Codebase health

Main branch is clean. All 9 migrations apply cleanly. Session context (step-15/16)
is functional. CI green across last 5 runs. No flakiness detected.
