# Pipeline Review — 2026-03-28 (automated)

## Action taken

- **Blocked PR #23** (step-17 bug fix): Posted review comment — do NOT merge. The PR moves `tasks/done/step-18.md`, `tasks/done/step-19.md`, and `tasks/done/step-20.md` back to `tasks/pending/`, which would corrupt pipeline state. Actual code changes are minor (CI workflow lint targets + BUGS_AND_ISSUES.md cleanup). The step-17 bug fixes described in the PR body do not appear in the file diff.
- **Merged PR #24** (step-21 — LLM Routing Fallback Classifier): All 4 CI checks passing (lint, typecheck, secrets-check, test). Implementation clean and matches spec exactly. Confirmed `"merged": true` from GitHub API. Merge SHA: 231b70350aed68e87477cc870544e4c794fe4504.
- **Moved** `tasks/triggered/step-21.md` → `tasks/done/step-21.md`
- **Queued step-22**: Per-user Telegram session isolation. Spec written to `tasks/pending/step-22.md`.

## Pipeline state

- Last merged: Step 21 — LLM Routing Fallback Classifier (PR #24, merged 2026-03-28)
- In flight: none (tasks/triggered/ is empty)
- Queued: step-22.md in tasks/pending/ (NucBox will fire Jules via AUTO_CREATE_PR)
- Blocked: PR #23 — step-17 bug fix branch has task file regression (steps 18-20 moved back to pending). Left open for Daniel's review; recommend closing.

## PR #24 Review Notes (step-21)

Implementation exactly matched spec:
- `xibi/routing/llm_classifier.py` (140 lines): `LLMRoutingClassifier` with confidence threshold 0.75, hallucination guard (tool name validation against manifest), silent error handling.
- All 5 required unit tests present: high-confidence, low-confidence, hallucinated tool, model error, empty manifests.
- CLI integration in `xibi/cli.py`: LLM fallback triggers only when `ShadowMatcher` returns no match. Debug output shows `[llm:hint] skill/tool (confidence)`.
- CI: lint, typecheck, secrets-check, test — all passing.
- No new dependencies.

## PR #23 Review Notes (step-17 bug fix)

**Do not merge.** Issues:
1. Moves `tasks/done/step-18.md`, `done/step-19.md`, `done/step-20.md` → `tasks/pending/` (wrong direction — these are completed steps).
2. The four step-17 bug fixes described in the PR body (Migration 7 AttributeError, ReAct tool error detection, `step.duration_ms`, TelegramAdapter refactor) are **absent** from the file diff. No changes to `xibi/` source files.
3. The only code changes: minor CI workflow lint target update and 3 lines removed from BUGS_AND_ISSUES.md.

## Step-22 Spec Summary (queued)

**Per-user Telegram session isolation** — Replaces single-instance `_active_chat_id: int | None` and `_nudge_sent: bool` in `TelegramAdapter` with a per-chat-id dict `_active_chats: dict[int, dict]`. This prevents the nudge timer from firing on the wrong user's chat if two messages arrive before the first response completes. Session context (`_get_session`) is already per-chat-id; this step closes the remaining nudge state leak. Adds 4 targeted integration tests. No new dependencies.

## Notes

- git log confirms merge at HEAD: `231b703 Merge PR #24: LLM Routing Fallback Classifier (step-21)`
- PR #23 left open (not closed) — flagged for Daniel's review with explanation comment.
- tasks/done/ now contains step-01 through step-21 (plus step-01b, step-11b).
- CI on main: all checks passing on the merge commit.
