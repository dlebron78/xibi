# Pipeline Review — 2026-04-04 05:46

## Action taken

Rejected PR #50 — Opus code review returned REJECT due to critical architecture violation (bare `sqlite3.connect()` in `send_email.py::_track_outbound()`). Review comment posted to PR. No merge. Specs remain in triggered/.

## Pipeline state
- Last merged: Step 45 (from prior run)
- In flight: PR #50 (step-46 content, mislabeled as step-46a)
- Next up: step-46a-patch-nudge-wiring still pending (no PR); step-46b and step-47 in pending/
- Queued (blocked): step-48-multi-source-framework (depends on step-47, which is in pending/ not done/)
- Fired Jules: No — Opus REJECTED, cannot merge

## Spec Mismatch Note

PR #50 is titled "Patch: Wire nudge() into Executor" (matching `tasks/triggered/step-46a-patch-nudge-wiring.md`) but its diff implements **step-46 (Centralized Entity & Contact System)**. The PR contains no nudge manifest, no nudge tool — only entity resolver, contacts migration, session bridge, and outbound tracking. The Opus review was conducted against step-46.

This means:
- step-46 implementation exists in PR #50 — awaiting one fix then re-review
- step-46a (nudge wiring) is still unimplemented — no PR exists for it yet
- Both are in tasks/triggered/ simultaneously, which is correctly the state while PR #50 is open

## Opus Review — PR #50 (step-46 content)

**Verdict: REJECT**

### Spec alignment
- ✅ Goal implemented: unified contact model with cross-channel identity resolution
- ✅ xibi/entities/__init__.py created (create_contact, upsert_contact_channel)
- ✅ xibi/entities/resolver.py created (resolve_contact, Contact dataclass, 3+1 resolution strategies)
- ✅ xibi/db/migrations.py: migration 18 added (contacts extensions + contact_channels table + session_entities.contact_id)
- ✅ xibi/signal_intelligence.py: upsert_contact() updated to use resolver + domain inference
- ✅ xibi/session.py: bridge_to_contacts() added, get_context_block() enriched
- ✅ skills/email/tools/send_email.py: _track_outbound() added, send_smtp() calls it
- ✅ skills/email/tools/reply_email.py: _workdir passed for DB lookup
- ✅ 14 tests present (12 required): test_entity_resolver.py (6), test_contact_enrichment.py (4), test_session_entity_bridge.py (4)
- ✅ Constraints: no LLM calls in resolution path, migration backwards-compatible, no breaking changes

### Architecture alignment
- ✅ No model name references
- ✅ Python/Role boundary preserved — resolution is pure Python
- ❌ CRITICAL: send_email.py::_track_outbound() uses bare sqlite3.connect() instead of open_db() — violates WAL/timeout enforcement requirement
- ✅ Terminology correct throughout
- ✅ No asyncio
- ✅ No MCP tools
- ✅ session_turns source tagging unaffected

### Test quality
- ✅ Tests assert meaningful behavior (DB state verification, not just "runs without error")
- ✅ Failure paths covered: ambiguous match returns None, single-session mention creates no contact
- ✅ No real model calls — all in-memory SQLite fixtures
- ✅ 14 tests >= 12 required

### Operational readiness
- ❌ FAIL: send_email.py::_track_outbound() — bare sqlite3.connect(db_path) in two places inside the function. No WAL mode, no timeout. Must use open_db(Path(db_path)).
- ⚠️ CONCERN: Silent exception swallowing in _track_outbound() — `except Exception: pass` hides errors. Should log with exc_info=True.
- ✅ No new background threads
- ✅ Migration runs once at startup
- ✅ No new env vars without fallback
- ✅ Context managers used (clean shutdown)

### Vision notes
- Step-46 is foundational and correct — unified contacts enables tier resolution, observation cycle enrichment, and lead nurturing. Good investment.
- Undocumented 4th resolution strategy (name-only match after name+org): not in spec, may cause false positives if two contacts share a display_name but different orgs. Should add test for collision or remove.
- After fix, step-46 is solid and ready to ship.

### Summary
PR #50 implements step-46 (Centralized Entity & Contact System) under a mismatched title. The feature work is comprehensive and well-tested: 14 tests covering happy paths, ambiguous cases, partial contact creation, and context enrichment. All required files are present. The sole rejection cause is that `_track_outbound()` in `send_email.py` uses `sqlite3.connect()` directly instead of `open_db()`, which bypasses the WAL mode and timeout enforcement that the architecture requires for concurrent DB access. This is a one-line fix per write path. Once corrected, CI re-runs, and the next pipeline pass will re-review.
