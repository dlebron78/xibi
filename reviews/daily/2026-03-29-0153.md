# Pipeline Review — 2026-03-29 05:48

## Action taken
**Merged PR #31 + queued step-29 spec.**

1. PR #31 ("Command Layer + Schema Validation + Action Dedup") was open with all CI passing
   (test, typecheck, lint, secrets-check — all green). Mergeable state: clean.
2. Reviewed the diff: `xibi/tools.py`, `xibi/command_layer.py`, `xibi/react.py` changes,
   `tests/test_tools.py`, `tests/test_command_layer.py`. All 23 test cases from spec present
   and correct. Implementation matches spec exactly — purely additive, no Executor changes,
   `dispatch()` fully backward-compatible.
3. Merged via GitHub API → `"merged": true` (sha: ca2ab5f330581e5414b10f998de4e4ecb7b8e985).
4. Moved `tasks/triggered/step-28.md` → `tasks/done/step-28.md`.
5. Wrote `tasks/pending/step-29.md` — Observation Cycle MVP (`xibi/observation.py`).

## Pipeline state
- Last merged: Step 28 — Command Layer + Schema Validation + Action Dedup (PR #31, merged 2026-03-29 ~05:48 UTC)
- In flight: none (tasks/triggered/ is empty)
- Queued: `tasks/pending/step-29.md` — Observation Cycle MVP

## PR #31 code quality notes
- `resolve_tier()` demotion guard is correct: compares tier_order values, only accepts override if strictly less restrictive.
- `CommandLayer.check()` gate order matches spec: schema → permission → dedup → allow.
- `_check_dedup()` query correctly uses `chat_id = 'tool:nudge'` to match what `audit()` writes.
- `validate_schema()` handles bool/int distinction correctly (Python's bool is a subclass of int — the implementation checks `isinstance(value, bool)` before `isinstance(value, int)` for the integer type check).
- `react.py` change: the fallback path after command_layer check (`if executor is not None else stub`) is correct. No regression to the non-command_layer path.
- One minor observation: `audit()` reuses the `access_log` table's `chat_id`/`authorized`/`user_name` columns with non-standard semantics (storing payload in `user_name`). This is the agreed approach from the spec ("uses existing access_log table"). Acceptable for now; a dedicated `command_audit_log` table would be cleaner but is out of scope for this step.

## Step-29 spec notes
Step-29 is the Observation Cycle MVP (`xibi/observation.py`). Key design decisions in the spec:
- Single-pass only — no two-pass pre-filtering at email volume.
- `get_model("text", "review")` primary, "think" fallback, reflex-only as last resort.
- Schema migration 11 adds `observation_cycles` table for watermark + cycle history.
- `HeartbeatPoller` gets an optional `observation_cycle=` hook (additive, no breaking changes).
- `CommandLayer(interactive=False)` is required — observation is always headless.
- Hard cap: 100 signals/cycle, 5 tool calls/role run, 3 tool calls/reflex run.
- 23 test cases specified covering trigger logic, watermark, degraded mode fallthrough,
  reflex keyword matching, and migration verification.

## Notes
- Step-28 was triggered at 01:30 AST (-0400) = 05:30 UTC. Jules had it done in ~10 minutes —
  PR #31 opened at 05:40 UTC, CI passed ~8 minutes later. Clean and fast.
- Step-29 spec is in tasks/pending/. NucBox cron should fire Jules within 30 minutes.
- No architectural concerns with step-28 implementation.
- No merge conflicts (only one PR was open, and it was clean).
- CI is green on main after merge.
