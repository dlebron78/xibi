# Pipeline Review — 2026-03-30 00:38

## Action taken
Rejected PR #40 (Step 36: Xibi Deployment / NucBox Cutover) — 1 hard blocker remains unresolved (DEPLOY.md missing). Posted rejection comment via GitHub API.

## Pipeline state
- Last merged: Step 35 — (last entry in tasks/done/)
- In flight: PR #40 (step-36) — REJECTED round 2, awaiting Jules fix
- Queued: none (step-36 spec in tasks/triggered/, no new spec to write)

## Opus Review — PR #40 (Step 36)

**Verdict:** REJECT

### Previous blockers resolved in this round
- ✅ **Blocker #1 (MCPServerRegistry NameError)**: FIXED — `from xibi.mcp.registry import MCPServerRegistry` is properly imported at line 13 of `xibi/__main__.py`
- ✅ **Blocker #2 (Hardcoded paths)**: FIXED — Both `systemd/xibi-telegram.service` and `systemd/xibi-heartbeat.service` use systemd specifiers `%U` and `%h` instead of `/home/dlebron`. Verified by new test `test_service_files_use_specifiers`.

### Remaining hard blocker

**DEPLOY.md not added to repo.**

The spec explicitly requires: *"Modified: `DEPLOY.md` — Add new section 'Xibi Deployment (Current)' above existing Bregger section, documenting config location, workdir, service names, useful commands (logs, restart, rollback), and migration path from Bregger."*

`DEPLOY.md` does not appear in the PR diff and does not exist anywhere in the git history. Jules' previous response claimed this was fixed, but the diff does not contain any DEPLOY.md changes. This is a required deliverable per spec.

### Spec alignment
- ✅ `systemd/xibi-telegram.service` — present, uses specifiers
- ✅ `systemd/xibi-heartbeat.service` — present, uses specifiers
- ✅ `scripts/xibi_cutover.sh` — present, dry-run mode works, idempotent
- ✅ `scripts/xibi_rollback.sh` — present, correct
- ✅ `scripts/xibi_config_migrate.sh` — present
- ✅ `scripts/xibi_config_migrate.py` — present, non-destructive migration
- ✅ `scripts/xibi_deploy.sh` — updated with backward-compatible `is-enabled` guard
- ✅ `xibi/__main__.py` — heartbeat subcommand added, `--config` global arg added
- ✅ `tests/test_step36_deployment.py` — 3 tests present (spec required 2)
- ❌ `DEPLOY.md` — NOT IN DIFF, NOT IN REPO

### Architecture alignment
- ✅ No model-by-name references in Python code (migration script uses config defaults only, not runtime)
- ✅ Python stays in collect/route/enforce role
- ✅ No new SQLite state
- ✅ Terminology correct (adapter, channel, transport)
- ✅ No asyncio introduced
- ✅ No MCP tools in scope
- ✅ No beliefs/session_turns touched

### Test quality
- ✅ `test_cutover_script_dry_run`: Runs script twice, strips timestamps, compares output for idempotency — excellent approach
- ✅ `test_config_migrate_produces_valid_schema`: Validates JSON output structure, key mappings, legacy preservation
- ✅ `test_service_files_use_specifiers`: Reads actual files, asserts specifiers present, asserts hardcoded paths absent
- ✅ All 3 tests assert meaningful behavior, no real model calls, failure paths covered

### Vision notes
- This step correctly enables safe production deployment for a personal agent running on NucBox
- Dry-run mode, rollback path, and config migration all reduce deployment risk
- Code quality is high — shell scripts use `set -euo pipefail`, proper quoting, timestamps
- One limitation: sleep delays (sleep 3) between service starts rather than health checks — acceptable for NucBox
- One limitation: heartbeat poller not tested directly, but this is not required by spec

### Summary
Jules has fixed two of three previous blockers well. The MCPServerRegistry import is in place, service files properly use systemd specifiers, and the test suite is solid with idempotency coverage added. The single remaining blocker — DEPLOY.md not committed — is straightforward to fix. The PR is one commit away from approval once DEPLOY.md is added to the repository with the required "Xibi Deployment (Current)" runbook section. Rejection comment posted to PR #40 with precise instructions.
