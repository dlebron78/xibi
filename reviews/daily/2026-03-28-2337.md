# Pipeline Review — 2026-03-28 (automated run)

## Action taken
Merged PR #28 (step-25: Cross-Session Memory Compression) → queued step-26 spec to tasks/pending/

## Pipeline state
- Last merged: Step 25 — Cross-Session Memory Compression (PR #28, merged this run)
- In flight: none
- Queued: tasks/pending/step-26.md — Standardize SQLite Connections via `open_db()`

## PR #28 Review (step-25)

**CI:** All checks passed — typecheck ✅, secrets-check ✅, test ✅, lint ✅
**Mergeable state:** clean (no conflicts)
**Review verdict:** CLEAN — merged automatically.

Implementation matches spec exactly:
- `COMPRESS_WINDOW = 8`, `MAX_BELIEFS = 5` constants present
- `compress_to_beliefs()` method: dedup sentinel, confidence gate (≥ 0.75), valid_until 30 days, never raises
- `_get_session_memories()` method: fetches up to 5 non-expired `session_memory` beliefs
- Trigger in `get_context_block()` on stale session (> 30 min) — calls `compress_to_beliefs()` then returns ""
- Memory injection in `get_context_block()` appended under "What I remember from before:" header
- All 5 required tests present and passing

## Step 26 Spec Rationale

`open_db()` in `xibi/db/__init__.py` already provides WAL mode, `busy_timeout=5000`, and
`check_same_thread=False`. However, 32 raw `sqlite3.connect()` calls across five hot-path
modules bypass it entirely:
- `session.py`: 8 calls (memory compression just added, still uses raw connect)
- `alerting/rules.py`: 11 calls (one has custom `isolation_level=None`)
- `trust/gradient.py`: 3 calls
- `tracing.py`: 4 calls
- `channels/telegram.py`: 6 calls (inconsistent timeouts: 10s, default, 10s)

Under concurrent load (Telegram poller + heartbeat + ReAct all writing simultaneously),
raw connections without WAL mode create lock contention and potential write failures. This
is the P2 backlog item "SQLite connection pooling and WAL mode".

Step-26 is a mechanical refactor with no behavioral changes and 3 new WAL verification tests.

## Notes
- Merge of PR #28 confirmed: `merged: true`, SHA `b5bc0f364370fc206c3633e8b05183e29706e2d2`
- `tasks/done/step-25.md` created, `tasks/triggered/step-25.md` removed
- Previous NucBox stall (step-25 spent ~10h in pending before triggering) now resolved
- Pipeline health: steps 1–25 all in tasks/done/, no open PRs, CI green on main
- step-26 queued in tasks/pending/ — NucBox should pick up within 30 minutes
