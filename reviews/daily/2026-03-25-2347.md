# Pipeline Review — 2026-03-25

## Action taken

Merged PR #7 ("Implement Telegram Bot Adapter") and queued step-07 spec.

## Pipeline state

- Last merged: Step 06 — Telegram Channel Adapter (PR #7, SHA 10711f7)
- In flight: none
- Queued: tasks/pending/step-07.md — Heartbeat Daemon

## PR #7 Review Summary

**All CI checks passed:** lint ✅ / secrets-check ✅ / typecheck ✅ / test ✅

**Implementation vs. spec — clean match:**

- `xibi/channels/telegram.py` (289 lines): all required methods present —
  `is_continuation()`, `extract_task_id()`, `TelegramAdapter.__init__()`,
  `_load_offset()`, `_save_offset()`, `_api_call()`, `_mock_api_call()`,
  `send_message()`, `_on_react_step()`, `_download_file()`, `is_authorized()`,
  `_process_message()`, `poll()`. Zero external dependencies (stdlib only). ✅
- `_process_message()` was extracted as a private helper — good factoring, cleaner
  than the monolithic poll() in Bregger. ✅
- `_mock_sent` and `_pending_attachments` are instance attributes (no module globals). ✅
- `from __future__ import annotations` at top. ✅
- All 17 spec-required tests present and passing. ✅
- No hardcoded secrets or tokens. ✅

**Minor observations (not blocking):**
- `_api_call` uses bare `except (urllib.error.URLError, Exception)` — the second
  clause makes the first redundant. Harmless but could be tightened to just `except Exception`.
- `send_message()` return type annotated as `dict` (without type params) — acceptable.

**Verdict: merge approved.**

## Step 07 Spec

Queued `tasks/pending/step-07.md` — Heartbeat Daemon.

Scope: ~1,421 lines of `bregger_heartbeat.py` modularized into:
- `xibi/alerting/rules.py` — `RuleEngine` (SQLite rule store, triage log, signal log, watermark)
- `xibi/heartbeat/poller.py` — `HeartbeatPoller` (tick loop, email triage, digest/recap/reflection)

Key design decisions in spec:
- `TelegramNotifier` class NOT ported — `TelegramAdapter.send_message()` reused directly
- No hardcoded model names — `get_model(tier="local")` for all LLM classification
- All 5 SQLite tables (`rules`, `triage_log`, `heartbeat_state`, `seen_emails`, `signals`) encapsulated inside `RuleEngine`
- 23 tests required (10 for `RuleEngine`, 13 for `HeartbeatPoller`)

## Notes

Steps done: 01, 01b, 02, 03, 04, 05, 06 (7 total)
Steps remaining in P2 tier: 07, 08, 09
Steps deferred (P3): 10 (Observability Dashboard)
