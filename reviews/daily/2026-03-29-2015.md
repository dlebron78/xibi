# Pipeline Review — 2026-03-30 00:04 UTC

## Action taken
Rejected PR #38 (step-35 — MCP Foundation) — step-35a Fix 2 and Fix 3 missing; test_safety_remediation.py absent. Comment posted to PR #38. Noted PR #39 (CI failing, redundant). No merge. Specs remain in tasks/triggered/ (step-35.md and step-35a.md).

## Pipeline state
- Last merged: Step 34 — Dashboard Modernization (PR #37)
- In flight: PR #38 (step-35 — MCP Foundation) — REJECTED awaiting step-35a Fix 2 + Fix 3 + tests | PR #39 (step-35 alternate) — CI failing (lint, typecheck)
- Queued: step-36.md (tasks/pending/) — Xibi deployment and NucBox cutover from Bregger
- Triggered: step-35.md, step-35a.md (both in tasks/triggered/)

## Situation Assessment

### Two competing PRs
- **PR #38** (`mcp-foundation-11190788882875162637`): 9159-line diff. CI passing (test ✅, lint ✅, typecheck ✅, secrets ✅). Updated after previous rejection with DEFAULT_TIER = RED fix. This is the primary candidate.
- **PR #39** (`mcp-foundation-9156793708217061001`): Clean rewrite, CI failing (lint ❌, typecheck ❌). Submitted at 23:55 UTC, redundant. Comment posted recommending closure in favor of PR #38.

### Merge conflict status
Neither PR is in a dirty/conflict state. PR #38 mergeable: unknown (GitHub still computing). PR #39 mergeable: True / state: unstable (CI failure).

---

## Opus Code Review — PR #38 (Step 35 — MCP Foundation, post-fix)

**Verdict: REJECT** — step-35a prerequisites incomplete

### Spec Alignment — step-35

✅ **MCPClient** (`xibi/mcp/client.py`, 225 lines) — initialize(), call_tool(), close(), is_alive() all present. Correct synchronous blocking I/O via select.select(). Timeout returns `{"status": "error", "error": "timeout"}`. isError:true normalized correctly. Response truncation at max_response_bytes working.

✅ **MCPServerRegistry** (`xibi/mcp/registry.py`, 113 lines) — Reads `config["mcp_servers"]`, creates MCPClient per server, groups tools into skill-level manifests ({"name": "mcp_filesystem", "description": ..., "tools": [...]}), calls skill_registry.register(). Failing server logged and skipped, does not abort startup. Tool name collision handled via server__tool namespacing with WARNING logged.

✅ **MCPExecutor** (`xibi/executor.py`) — can_handle() searches skill manifests prefixed "mcp_"; execute() looks up server from tool manifest's "server" field, uses "original_name" for actual MCP call (correctly handles namespaced tools). Wired into Executor.execute(): MCP checked before local; local wins on collision with WARNING.

✅ **SkillRegistry.register()** (`xibi/skills/registry.py`) — Added. Takes full skill manifest (name + tools list), stores as SkillInfo. MCPServerRegistry correctly passes skill-level manifest format.

✅ **DEFAULT_TIER = PermissionTier.RED** (`xibi/tools.py:14`) — FIXED. MCP tools not in TOOL_TIERS → get DEFAULT_TIER = RED via resolve_tier(). Tier enforcement is correct even though "tier": "RED" in manifest is unused by permission system.

✅ **Schema rename** ("parameters" → "inputSchema") — All 5 skill manifests (calendar: 3 tools, email: 10, filesystem: 4, memory: 5, search: 4) updated. react.py line 90 uses `tool_manifest.get("inputSchema")`. validate_schema() updated.

✅ **config.example.json** — mcp_servers block added with filesystem example.

✅ **No asyncio** — select.select() used throughout.

✅ **source: "mcp"** on every injected tool manifest.

✅ **Failing server logged and skipped** — exception in initialize_all() per server is caught, logged as ERROR, continues.

✅ **tests/test_mcp.py** — 8 tests present and named correctly:
  - test_mcp_client_initialize_success ✅
  - test_mcp_client_call_tool_success ✅
  - test_mcp_client_tool_error_normalized ✅
  - test_mcp_client_timeout ✅ (mocks select.select to return empty)
  - test_mcp_client_response_truncated ✅
  - test_mcp_registry_injects_tools ✅
  - test_mcp_registry_server_failure_does_not_abort ✅
  - test_mcp_tool_name_collision_namespaced ✅

✅ **tests/test_schema_validation.py** — 5 tests present (minor naming differences: snake_case vs camelCase, functionally correct):
  - test_local_tool_valid_input_passes_validation ✅
  - test_local_tool_invalid_input_caught ✅
  - test_missing_input_schema_field_returns_empty ✅
  - test_all_local_skill_manifests_have_input_schema ✅
  - test_react_loop_validates_tool_input ✅

### Spec Alignment — step-35a (PREREQUISITE — must merge before step-35)

✅ **Fix 1: DEFAULT_TIER = RED** — Present in tools.py:14.
✅ **test_default_tier_for_unknown_tool** (as test_unknown_tool_defaults_red) — Updated to assert RED in test_tools.py.
✅ **test_known_green_tools_unchanged** — Added to test_tools.py.

❌ **test_known_red_tools_unchanged** — Missing from test_tools.py (required by step-35a Fix 1 test suite).

❌ **Fix 2: _run_reflex_fallback() trust wiring** — NOT PRESENT.
   - `xibi/observation.py` `_run_reflex_fallback()` signature unchanged (no trust_gradient param)
   - Call site (line ~257) still passes only (signals, executor, command_layer)
   - No PERSISTENT failure recording for text.review and text.think on worst-case fallback
   - 4 tests in test_safety_remediation.py for this fix: all missing

❌ **Fix 3: session_turns source column** — NOT PRESENT.
   - SCHEMA_VERSION = 14, no migration 15
   - CREATE TABLE session_turns (line 267) has no source column
   - INSERT INTO session_turns (line 179) doesn't include source
   - SessionTurn dataclass has no source field
   - 4 tests in test_safety_remediation.py for this fix: all missing

❌ **tests/test_safety_remediation.py** — File does not exist. 11 required tests absent.

### Architecture Alignment

✅ Code never references models by name — uses get_model(specialty, effort)
✅ Python routes/enforces; Roles reason — boundary maintained
⚠️ session_turns source column NOT added — architecture doc states "Every session turn sourced from MCP tool response carries 'source': 'mcp'" for belief poisoning mitigation. Column is missing. Step-35 spec defers this, but step-35a spec requires it as a prerequisite.
✅ Channel/tool/skill/transport/adapter terminology matches architecture doc
✅ All MCP tools default to RED tier (via DEFAULT_TIER = RED, functionally correct)
✅ No asyncio introduced
⚠️ Beliefs/session_turns: source tagging infrastructure missing (step-35a Fix 3 required)

### Test Quality

✅ Tests assert meaningful behavior (not just "runs without error")
✅ Failure paths covered: isError:true, timeout (select.select returns empty), server init failure, large response truncation
✅ No real model calls — all mocked (unittest.mock, MagicMock)
✅ Test count: 13 total (8 MCP + 5 schema) meets spec requirement
⚠️ test_mcp_tool_name_collision_namespaced: assertion is slightly loose (`"server1__read_file" in names or "read_file" in names`) — could pass without actually testing namespacing. Acceptable but noted.
⚠️ test_mcp_registry_injects_tools: checks manifest["tier"]=="RED" but doesn't verify resolve_tier() returns RED. Acceptable since DEFAULT_TIER = RED covers this globally.

### Critical Integration: Tier Enforcement

The "tier": "RED" in synthetic tool manifests is NOT read by command_layer → resolve_tier(). This is acceptable because:
1. DEFAULT_TIER = PermissionTier.RED (now fixed)
2. MCP tools are not in TOOL_TIERS
3. resolve_tier(mcp_tool_name) → TOOL_TIERS.get(name, DEFAULT_TIER) → RED ✅

The manifest's "tier" field is unused but serves as documentation. No functional gap.

### Vision Notes

The step-35 MCP Foundation is well-built: synchronous blocking client, proper error normalization, registry/executor cleanly integrated, schema rename finally makes validation functional (was silently skipped everywhere before this PR). 

The missing step-35a items (Fix 2 and Fix 3) are not MCP-specific — they are independent safety improvements that should have landed first. Fix 3 (session_turns source column) is particularly important for the belief poisoning mitigation described in the architecture doc: without it, MCP tool responses that enter session_turns cannot be tagged as "mcp" source, which is the foundation of Phase 1 belief protection.

Daniel should know: PR #38's MCP implementation is correct and ready. The only blockers are the step-35a items that observation.py and session.py are still missing. These are mechanical additions — no design decisions required.

### Summary

PR #38 delivers a complete, correct MCP Foundation implementation (step-35) and correctly applies step-35a Fix 1 (DEFAULT_TIER = RED). The CI passes cleanly across all 4 checks. However, step-35a Fixes 2 and 3 (observation.py reflex trust wiring and session_turns source column + migration) are absent, as is test_safety_remediation.py with its 11 required tests. Step-35a must be fully merged before step-35 can be approved. Do not merge.

---

## Notes

### PR #39 (competing step-35 submission)
PR #39 was submitted at 23:55 UTC as a clean rewrite. CI is failing (lint and typecheck). A comment was posted recommending closure in favor of PR #38, which has CI passing and more complete implementation. If Jules prefers PR #39's approach, CI must pass first and the step-35a items must still be included.

### step-35a trigger timing
step-35a was triggered at 19:00 EDT (23:00 UTC) today — approximately 64 minutes before this pipeline run. Per Case C rules, Jules may still be working on it (< 2 hours). However, Jules submitted PR #39 (step-35 again) rather than implementing step-35a items. This is a pipeline ordering problem — Jules triggered step-35 before completing step-35a prerequisites.

### Recommendation for next pipeline run
If PR #38 or PR #39 gains a new commit adding step-35a Fixes 2 and 3 with test_safety_remediation.py, re-run Opus review. The step-35 portions need no re-review. Only the step-35a additions need verification.

If > 2 hours pass with no PR addressing step-35a: flag as stall, do not re-trigger.
