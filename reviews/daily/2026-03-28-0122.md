# Pipeline Review — 2026-03-28 09:45

## Action taken

**Cleaned up stale task files** — unblocking NucBox for step-17.

`tasks/triggered/step-15.md` and `tasks/pending/step-16.md` were left behind by the
previous Cowork run (2026-03-28-0110). That run merged PR #18 (steps 15+16) and added
the files to `tasks/done/`, but did NOT remove the originals from triggered/ and pending/.

As a result, NucBox's `--check-pending` cron would find `step-16.md` as the lexically
first pending spec and attempt to fire Jules on an already-completed step. Meanwhile,
triggered/ showed step-15 as in-flight, creating a confusing state.

Both stale files removed and pushed. NucBox will now find `step-17.md` as the first
pending spec on its next run (~within 30 min).

## Pipeline state

- Last merged: Step 15+16 — Session Context (turn management + entity extraction), PR #18
- In flight: none
- Queued (tasks/pending/): step-17, step-18, step-19, step-20
- tasks/triggered/: empty — NucBox will fire step-17 next run

## Step-17 pre-assessment (for Jules's benefit)

Step-17 covers 4 confirmed bugs. Current state:

**Bug 1 (`_migration_7` outside class):** Already fixed by Cowork in commit 5576df3
(Mar 27). `_migration_7` is now at line 252 inside `SchemaManager`. Jules should
verify but no action needed.

**Bug 2 (tool error detection too narrow):** Still present in `xibi/react.py` line
280-281. The `_xibi_error` gate means plain `{"error": "..."}` tool responses never
populate `step.error`. Needs the broader 3-branch fix from the spec.

**Bug 3 (step timing excludes tool execution):** Still present. `step.duration_ms` is
set at Step construction (after LLM parse, before dispatch). One-line fix after
`step.tool_output = tool_output`.

**Bug 4 (Telegram uses non-existent method):** Partially addressed by PR #18 — Telegram
now calls `react.run()` directly when core exposes registry/executor/config. But
`self.core` still exists and `process_query()` fallback remains. Full refactor to
accept config/registry/executor directly (remove `self.core`) not yet done.

## CI health

CI green on main (546 tests, 0 failures) as of run #23678149958 at 2026-03-28T05:10:53Z.

## Notes

The 8.5-hour gap between PR #18 merge (01:10) and this run was caused entirely by the
stale triggered/step-15.md and pending/step-16.md. The cleanup was the only action needed.
