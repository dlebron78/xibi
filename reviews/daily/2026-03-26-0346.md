# Pipeline Review — 2026-03-26 03:43

## Action taken

Merged PR #11 (step-09 — MessageModeClassifier Redesign) and queued step-10 spec.

## Pipeline state

- Last merged: Step 09 — MessageModeClassifier Redesign + ShadowMatcher Update (PR #11)
- In flight: none
- Queued: step-10 (Observability Dashboard) → `tasks/pending/step-10.md`

## PR #11 Review Notes

**CI**: all 4 checks passed (typecheck, lint, test, secrets-check). Mergeable state: clean.

**Implementation quality**: Clean and spec-compliant.

- `xibi/routing/classifier.py` — `ModeScores` dataclass + `MessageModeClassifier` with
  keyword scoring, shadow integration, question-mark heuristic. No external deps, no mutable
  module state. `from __future__ import annotations` throughout. ✅
- `xibi/routing/shadow.py` — `extract_tool_input()` added correctly. `ShadowMatch.tool_input`
  field populated for direct-tier matches. Word-boundary regex removal is exact match to spec. ✅
- `xibi/react.py` — Bug fix: `dispatch(match.tool, {}, ...)` → `dispatch(match.tool, match.tool_input, ...)`.
  This fixes the silent empty-input gap for BM25 direct matches. ✅
- `xibi/routing/__init__.py` and `xibi/__init__.py` — Exports updated correctly. ✅
- `tests/test_classifier.py` — All 18 required test cases present. Tests use `unittest.mock.MagicMock`
  for shadow dependency. Note: tests mock `self.shadow.match()` (the actual method name in `ShadowMatcher`)
  rather than `self.shadow.query()` as the spec mentioned — this is correct for the implementation. ✅

One minor note: multi-word conversation keywords ("tell me", "can you", etc.) are matched with
`\btell me\b` which relies on word-boundary anchors at start and end of the phrase — this works
correctly because spaces inside the phrase don't affect the `\b` anchors at the phrase edges.

**Verdict**: clean merge. No architecture concerns.

## Step 10 Spec Notes

Step 10 (Observability Dashboard) writes `xibi/dashboard/app.py` using Flask app factory
pattern. Decision: Option A (keep Flask). Chosen because:
- heartbeat (step-07) and SQLite schema (step-08) are both merged and running
- frontend (`templates/index.html`) already exists and expects the same JSON API shape
- Option B (Grafana) would require infrastructure changes outside the codebase scope

Key spec constraints: read-only endpoints only, SQL in `queries.py` not inline, no legacy
`bregger_*.py` imports, Flask + psutil to be added to `pyproject.toml` dependencies.
