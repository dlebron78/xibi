# Pipeline Review — 2026-03-27

## Action taken

Merged PR #15 (step-14: Architectural Resilience). Moved `tasks/triggered/step-14.md` → `tasks/done/step-14.md`. No new spec queued — `tasks/pending/` already has step-15.md and step-16.md waiting for NucBox.

## Pipeline state

- Last merged: Step 14 — Architectural Resilience (Circuit Breakers, Timeouts, Error Transparency)
- In flight: None (PR #15 merged this run)
- Queued in pending: step-15.md, step-16.md
- Queued in triggered (stale, no open PRs): step-11b.md, step-12.md

## PR #15 review notes

All 4 CI checks passed: lint ✓, typecheck ✓, test ✓, secrets-check ✓. Mergeable state was clean.

**Implemented (matches spec):**
- `xibi/errors.py` — XibiError, ErrorCategory with full user_message() dispatch
- `xibi/circuit_breaker.py` — SQLite-backed CircuitBreaker, FailureType enum, CLOSED/OPEN/HALF_OPEN state machine with persistence
- `xibi/router.py` — TimeoutsConfig, Config extended, get_timeout() helper, BreakerWrappedClient wrapping each provider
- `xibi/executor.py` — module-level _EXECUTOR (ThreadPoolExecutor, max_workers=8), per-tool timeout via manifest `timeout_secs`, near-saturation warning
- `xibi/types.py` — Step.error field, ReActResult.error_summary, user_facing_failure_message()
- `xibi/react.py` — error_summary collection from steps + consecutive_errors reset (step-12 Fix 1 absorbed)
- `xibi/cli.py` — error surfacing to terminal
- `xibi/channels/telegram.py` — graceful fallback: checks for process_query_to_result or falls back, sends failure message instead of silence
- `xibi/dashboard/queries.py` — get_circuit_breaker_states() query (confirmed in pull output)
- `config.example.json` — timeouts block added
- 12 tests in `tests/test_resilience.py`, all passing

**Minor gap (non-blocking):** The Telegram adapter uses a defensive `hasattr(core, "process_query_to_result")` pattern rather than calling `react.run()` directly. This is correct for the current architecture where core is an opaque wrapper; the spec's sample code was illustrative, not prescriptive. When core is refactored to expose ReActResult natively, this will clean up naturally.

## Stale triggered specs (step-11b, step-12)

Both were queued to Jules but never produced open PRs. Their core requirements were absorbed into later steps:

- **step-12 Fix 1** (consecutive_errors reset): Implemented in PR #15 / `react.py` line 295.
- **step-12 Fix 2** (health check timeout): Implemented — `router.py` lines 358, 381 use `timeout=5` and `timeout=1`.
- **step-12 Fix 3** (heartbeat watermark race): NOT confirmed implemented. No WAL-mode transaction wrapping visible in `heartbeat/poller.py`. Low risk for single-user deployment.
- **step-12 Fix 4** (WAL mode on all connections): NOT implemented. `xibi/db/migrations.py` does not enable WAL. Medium risk on ungraceful shutdown.
- **step-11b Change 1** (probabilistic audit trigger): NOT implemented. `should_audit()` still uses deterministic `total_outputs % audit_interval == 0`. Low operational risk but causes predictable audit gaps.

**Recommendation:** Keep step-11b and step-12 in triggered for now. The remaining unimplemented items (WAL mode, probabilistic audit, watermark race) are real but not blocking. When step-15/16 PRs land and the backlog clears, revisit whether to fold these into a cleanup spec or address individually.

## Architecture notes

Step-14's BreakerWrappedClient pattern (inner class defined inside get_model()) is slightly unusual but correct — avoids defining a new module-level class just for wrapping. The class captures `breaker` and `inner` via closure, matching the ModelClient protocol. No issues.

The circuit breaker using `db_path = config.get("db_path") or Path.home() / ".xibi" / "data" / "xibi.db"` as default is sensible — avoids requiring db_path in all configs while keeping test isolation via tmp_path fixtures.
