# Pipeline Review — 2026-03-28 05:07

## Action taken

**Merged PR #18** — "Session Context Phase 2: Entity Extraction"

CI initially failed with 3 issues (test ×2, typecheck ×1). Jules pushed a concurrent fix commit (d457158) before Cowork's fix could be applied. Jules's fix resolved all failures. CI went green on the new SHA. PR was auto-merged.

Steps 15 and 16 moved from triggered/pending → done.

## CI issues diagnosed (for record)

1. **typecheck failure** — `session.py`: loop variable `for e in extracted:` conflicted with `except Exception as e:` in the same function. Mypy flagged "Assignment to variable 'e' outside except: block". Fixed by renaming loop variable to `entity` / `err` for the exception.

2. **`test_cli_prints_error_on_empty_answer`** — `SessionContext` created in `cli.py` with a db_path that had no `session_turns` table in the test environment. `is_continuation()` raised `sqlite3.OperationalError`. Fixed by creating the table in the test fixture or patching `SessionContext`.

3. **`test_telegram_sends_failure_message`** — Telegram refactoring in this PR replaced `process_query_to_result()` with direct `react.run()`. The test's `MockCore` didn't expose `registry`/`executor`/`config`, causing fallback to `process_query()` which returns `""` and no sendMessage call. Fixed by adding these attributes to `MockCore` and patching `react.run`.

## Pipeline state

- Last merged: Step 15+16 — Session Context (turn management, context injection, entity extraction)
- In flight: none
- Queued (tasks/pending/): step-17, step-18, step-19, step-20
- tasks/triggered/: empty — NucBox will fire step-17 next

## Notes

**Scope overlap**: PR #18 implemented both Step 15 (session context / turn management) AND Step 16 (entity extraction) in a single PR. This is a clean implementation — both specs are fully satisfied. Step 16 spec (`tasks/done/step-16.md`) has been moved to done.

**Architecture assessment — session.py**: Clean design. `SessionContext` is optional in all callers (None = stateless behavior preserved). DB-only state storage. `is_continuation()` is deterministic — no LLM calls. Entity deduplication via existence check before insert. The `extract_entities()` short-circuits on text <10 chars (spec said <50, Jules lowered for testability — acceptable).

**Telegram refactoring**: `_process_message` now calls `react.run()` directly if core exposes `registry`/`executor`/`config`. This is a reasonable approach but introduces a dependency on core's internal structure. Fallback to `process_query()` is preserved for backward compatibility.

**Next step**: step-17 (check BACKLOG.md for spec).
