# Pipeline Review — 2026-04-06 02:29 UTC

## Action taken

**Merged PR #55 after Opus APPROVE WITH NOTES, moved step-51 to done, wrote step-52 spec,
fired Jules immediately — session `17134852522356216777`.**

---

## Pipeline state

- Last merged: Step 51 — Heartbeat Resilience: Per-Phase Timeouts, Phase Isolation, Logging Fix
  (PR #55), merged 2026-04-06T02:17 UTC, merge SHA `446f2cc`
- In flight: `step-52-web-search-mcp-source` — Jules session `17134852522356216777`,
  triggered 2026-04-06T02:29 UTC
- Next up (pending): none — step-52 already promoted to triggered/
- Queued (blocked): none
- Fired Jules: **yes — step-52-web-search-mcp-source**, session `17134852522356216777`

---

## Merge Conflicts

PR #55 was in `mergeable_state: clean`. No conflicts. No rebase needed.

---

## Opus Review — PR #55 (Step 51)

**Verdict: APPROVE WITH NOTES**

### Spec alignment

- ✅ Fix 1 (per-phase timeouts): `_PHASE0_TIMEOUT_SECS=90`, `_PHASE1_TIMEOUT_SECS=10`,
  `_PHASE2_TIMEOUT_SECS=60`, `_PHASE3_TIMEOUT_SECS=180` defined as module-level constants
  in `xibi/heartbeat/poller.py`
- ✅ Fix 2 (Phase 3 subtask isolation): `_run_phase3()` extracted as `async def`, each
  of 3a/3b/3c/3d has independent `try/except` with `exc_info=True`
- ✅ Fix 3 (logging configuration): `logging.basicConfig(force=True, level=INFO)` in
  `cmd_heartbeat()` before any imports that trigger logging
- ✅ File structure: all three files in diff (poller.py, __main__.py, test file)
- ✅ Phase sequence preserved: 0→1→2→3
- ✅ Phase 1 synchronous: plain try/except, no asyncio wrapper
- ✅ Phase 2 synchronous timeout: wall-clock `time.monotonic()` check per iteration
- ✅ `_run_phase3()` is `async def`
- ✅ Constants are module-level
- ✅ `force=True` only in `cmd_heartbeat()`
- ✅ Existing `except Exception` in `run()` loop retained
- ✅ Type annotations on new public methods and constants
- ✅ 18 tests provided (spec required minimum 10)
- ⚠️ `_PHASE1_TIMEOUT_SECS = 10` defined but intentionally unused (Phase 1 is sync/fast);
  matches spec intent but is a code cleanliness issue (see Notes)
- ⚠️ `xibi/command_layer.py` modified with out-of-scope SQL formatting changes (harmless)

### Architecture alignment

- ✅ No model references by name — not applicable (operational hardening, no LLM calls)
- ✅ Python boundary not blurred — no reasoning added
- ✅ No new SQLite state
- ✅ Terminology correct
- ✅ No MCP tools involved
- ✅ Asyncio explicitly justified in spec Fix 1
- ✅ Beliefs/session_turns not touched

### Test quality

- ✅ 18 tests total, all meaningful assertions (not just "runs without error")
- ✅ Failure paths covered: Phase 0 timeout, Phase 0 exception, Phase 2 partial timeout,
  Phase 3 subtask crashes (sig_intel, observation, Jules watcher, Radiant each isolated),
  Phase 3 overall timeout, Phase 1 DB error, Phase 2 extraction exception, error-result
  skipping, optional-component skipping, sweep lifecycle errors
- ✅ No real model calls — all mocked
- ✅ Mock quality: `HeartbeatPoller.__new__()` pattern, comprehensive fixture injection

### Operational readiness

- ✅ DB writes: no new write paths; existing `open_db()` pattern preserved
- ✅ Exceptions in async phases caught with `exc_info=True`
- ✅ Not a new process — no contention analysis needed
- ✅ `logging.basicConfig()` called once at startup, not hot path
- ✅ No new env vars required
- ✅ Unclean shutdown: SIGTERM mid-tick leaves DB consistent (per-phase catches prevent
  partial-write propagation; Phase 3 subtask isolation means Radiant counter increment
  and audit reset are wrapped — no partial state on crash)
- ✅ Not a new service; graceful shutdown not in scope

### Vision notes

1. **Unused constant `_PHASE1_TIMEOUT_SECS`**: matches spec intent (Phase 1 is synchronous
   and fast, so no enforcement needed) but introduces a defined-but-unused constant.
   Either add a comment explaining it's defined for symmetry, or remove it. Recommend
   keeping with a clarifying comment since the set of four constants is used in tests
   and documentation.

2. **Phase 3 cancellation on timeout**: when Phase 3 times out, `asyncio.wait_for()` raises
   `CancelledError` inside `_run_phase3()`, cutting off any in-flight `await`. Sub-tasks
   that were mid-execution will not run their own cleanup path. This is operationally
   acceptable (no DB corruption, loop resumes next tick) but Daniel should know that a
   Phase 3 timeout may leave `obs_result` or observation cycle state partially computed.
   Logged at WARNING so post-mortems will see it.

3. **Out-of-scope formatting in `command_layer.py`**: SQL strings reformatted to single
   lines. Harmless, but noise in the diff. NFC on merge safety.

4. **Observation cycle logging truncated in diff**: the diff review showed `logger.info(...)`
   abbreviated. Assumed preserved from original; CI passing confirms no regression.

### Summary

Step-51 delivers three targeted operational hardening fixes to `HeartbeatPoller`: per-phase
timeouts (`asyncio.wait_for()` for Phases 0 and 3; wall-clock deadline for Phase 2; plain
try/except for Phase 1), Phase 3 sub-task isolation (sig_intel, observation cycle, Jules
watcher, and Radiant audit each in independent `try/except` in `_run_phase3()`), and
INFO-level journalctl visibility via `logging.basicConfig(force=True)` in `cmd_heartbeat()`.
All 18 tests pass CI. Architecture and operational readiness are sound. The two notes (unused
constant and Phase 3 cancellation behavior) are non-blocking trade-offs Daniel should be
aware of before the next heartbeat deployment.

---

## Step-52 Spec: Web Search MCP Source (Brave/Tavily)

**Rationale:** Step-51's explicit stated blocker was MCP source expansion latency. Now that
Phase 0 has a 90-second timeout, hanging sources cannot block Phase 3. Web search is the
first item in BACKLOG's MCP server sequence (read-only, API-key auth, no OAuth, lowest risk).
P1 items are exhausted; this is the highest-value unblocked P2 item and directly advances the
Chief-of-Staff and Tourism Chatbot use cases.

**Scope:**
- `web_search` signal extractor in `extractors.py` (structured search result → signal)
- `_url_to_ref_id()` and `_extract_domain()` helpers for stable dedup and entity naming
- `_poll_watch_topics()` in `source_poller.py` for profile-driven periodic searches
- `watch_topics` profile schema (query, interval_minutes, max_results)
- `source_metadata` context propagation in `async_tick()` so extractor knows the query
- 12+ tests in `tests/test_web_search_source.py`
- No new DB tables, no schema migration

**Fired Jules session:** `17134852522356216777`

---

## Post-Merge Verification

```
git log --oneline -3 origin/main:
  df4ba43 chore: trigger step-52-web-search-mcp-source — move pending → triggered
  a512d03 pipeline: step-51 done, step-52 web-search spec pending
  446f2cc Merge PR #55: Heartbeat Resilience: Per-Phase Timeouts, Phase Isolation, Logging Fix
```

Merge commit at top ✅. Step-52 spec in `tasks/triggered/` ✅.
