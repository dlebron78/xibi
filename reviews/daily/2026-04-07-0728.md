# Pipeline Review — 2026-04-07 07:28

## Action taken
Pipeline idle — no pending specs, no triggered specs, no queued specs, no open PRs. No action taken. Awaiting human direction.

## Pipeline state
- Last merged: Step 57 — memory-compression
- In flight: none
- Next up: none — no specs in pending/ or queued/
- Queued (blocked): none
- Fired Jules: no — pipeline idle

## Backlog (reference only — not converted to specs)
- `tasks/backlog/step-50-gaps.md` — prioritized dashboard/architecture fix list
- `tasks/backlog/step-58-mcp-collision-fix.md` — MCP synthetic-skill self-collision spec (see flag below)

These backlog items are noted for human awareness but are NOT being promoted. Spec authorship is reserved for humans and Opus.

## Opus Review
N/A — no open PR to review.

## ⚑ Flag for Daniel: step-58 direct commit — tests may be missing

Commit `26d90a2` (2026-04-07 01:08, authored by mrfresco@gmail.com) implemented the step-58 fix directly on `main`, bypassing the Jules → PR → Opus review flow. The source changes appear correct:

- `SkillInfo.source` field added (`"local"` | `"mcp"`)
- `SkillRegistry.register()` marks synthetic entries with `source="mcp"`
- `find_local_skill_for_tool()` helper ignores MCP-injected entries
- `executor.py` dispatch collision check updated to use the new helper

**However:** The commit message does not mention test additions, and the step-58 spec explicitly required three new tests:
1. `test_mcp_only_tool_routes_through_mcp_executor`
2. `test_real_local_skill_still_wins_over_mcp`
3. `test_mcp_tools_remain_visible_to_planner`

If CI passed without these tests, the acceptance criteria are not fully met. The spec also required:
- No `Tool file not found` error in `tool.dispatch` span for MCP-only tools
- No collision warning for MCP-only tools in `journalctl`

Recommendation: Daniel or Opus should verify whether the three specified tests exist in `tests/test_executor_core.py`. If not, a follow-up Jules task may be warranted. The `step-58-mcp-collision-fix.md` spec can be moved from `backlog/` to `done/` once tests are confirmed present — or left in backlog with a note if tests are deferred.

The pipeline driver does not make that call. This is flagged for human decision.

## Summary
Pipeline has been idle since step-57 merged (2026-04-07 ~00:00). step-58 was implemented as a direct commit by Daniel rather than through Jules, which is a valid human override but skips the Opus code review gate and potentially the test requirements from the spec. All other backlog items in `step-50-gaps.md` remain unscheduled — awaiting Daniel or Opus to author specs and place them in `tasks/pending/`.
