# Pipeline Review — 2026-03-26 05:08

## Action taken
Merged PR #12 (step-10 Observability Dashboard), moved spec triggered→done, queued step-11 spec.

## Pipeline state
- Last merged: Step 10 — Observability Dashboard (`xibi/dashboard/`)
- In flight: none (step-10 just merged, step-11 queued in tasks/pending/)
- Queued: step-11.md (Trust Gradient MVP)

## PR #12 Review Summary

**All CI checks passed:** lint ✅ typecheck ✅ secrets-check ✅ test ✅

**Implementation assessment:** Clean. Jules followed the spec closely:
- `DashboardConfig` dataclass with correct fields
- `create_app()` app factory pattern (no module-level Flask instance)
- All 8 routes implemented, SQL correctly isolated in `queries.py`
- Template folder resolved dynamically via `Path(__file__).parent.parent.parent / "templates"`
- Flask and psutil added to `pyproject.toml` dependencies
- 15 test cases cover all required scenarios
- `from __future__ import annotations` in all new files

**Minor observations (not blocking):**
- `queries.py:get_recent_errors()` contains a large inline comment explaining schema uncertainty — would be cleaner as a docstring note, but doesn't affect correctness
- `queries.py:get_recent_signals()` builds query with f-string using `PRAGMA table_info` output — values come from DB metadata not user input, so injection risk is nil, but worth standardizing in a future cleanup pass
- `/api/health` returns a few extra fields (`bot_alive`, `system_ram_pct`, `last_active`, `model_loaded`) beyond what the spec required — these are compatible additions that match the frontend's expectations from `bregger_dashboard.py`

## Step 11 Spec Written
Step 11 is "Trust Gradient MVP":
- `xibi/trust/gradient.py` — `TrustGradient`, `TrustRecord`, `TrustConfig` dataclasses
- DB migration: `trust_records` table (specialty, effort, audit_interval, consecutive_clean, total_outputs, total_failures)
- Promotion: double audit_interval after N consecutive clean outputs (cap at max)
- Demotion: halve audit_interval on any schema failure (floor at min)
- `should_audit(specialty, effort)` — boolean, 1-in-N sampling
- 22 unit tests, no external deps, no LLM calls
- Architecture reference: `public/xibi_architecture.md` § "Trust Gradient"

## Notes
- All 10 BACKLOG build queue steps are now done (step-01 through step-10)
- Next phase: roadmap Phase 2 / Step 11 onward (Trust Gradient → MCP Adapter → Specialty Dispatch)
- NucBox cron should pick up step-11.md within 30 minutes and fire Jules
