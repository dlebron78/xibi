# Pipeline Review — 2026-03-30 (automated)

## Action taken
Rejected PR #40 — Opus code review returned REJECT with 3 hard blockers. Comment posted to PR. Spec NOT moved to done/. No merge performed.

## Pipeline state
- Last merged: Step 35a — step-35 MCP Foundation Fix
- In flight: PR #40 (step-36, awaiting fixes per rejection comment)
- Queued: none

---

## Opus Review — PR #40 (Step 36: Xibi Deployment / NucBox Cutover)

**Verdict: REJECT**

CI Status: ✅ All 4 checks passing (typecheck, secrets-check, test, lint)
Opus review: ❌ 3 hard blockers

---

### Spec Alignment

- ❌ `DEPLOY.md` modification missing — spec explicitly requires adding a "Xibi Deployment (Current)" section above existing Bregger section documenting config location, service names, useful commands, and migration path. Diff includes no changes to DEPLOY.md.
- ❌ `cmd_heartbeat` uses `MCPServerRegistry(config, registry)` with no import — undefined name, fatal runtime error.
- ✅ `systemd/xibi-telegram.service` — present in diff, matches spec exactly
- ✅ `systemd/xibi-heartbeat.service` — present in diff, matches spec exactly
- ✅ `scripts/xibi_cutover.sh` — present, includes dry-run, pre-flight checks, correct structure
- ✅ `scripts/xibi_rollback.sh` — present, uses only local systemd commands (rollback-without-internet constraint met)
- ✅ `scripts/xibi_config_migrate.sh` — present
- ✅ `scripts/xibi_config_migrate.py` — present, maps `bregger["model"]` → `xibi["models"]["default"]`, preserves unknown fields under `_bregger_legacy`
- ✅ `scripts/xibi_deploy.sh` — updated with `is-enabled` guard (backward-compatible)
- ❌ Constraint violated: hardcoded `/home/dlebron/` paths in systemd service files break idempotency and portability

### Architecture Alignment

- ✅ No model-by-name references in new Python code
- ✅ No asyncio introduced
- ✅ No new SQLite state introduced
- ✅ Channel/tool/skill/transport terminology not misused in this step (infrastructure-only step)
- ✅ No MCP tools present — MCP tier constraint N/A
- ✅ No beliefs or session_turns touched
- ❌ MCPServerRegistry is called in `cmd_heartbeat` without import — represents a missing step-35 dependency being pulled in prematurely

### Test Quality

- ✅ 2 tests present (meets spec minimum of 2)
- ✅ `test_config_migrate_produces_valid_schema` — asserts meaningful behavior: JSON structure, required keys, model field mapping, `_bregger_legacy` presence
- ✅ No real model calls — all subprocess/fixture based
- ❌ `test_cutover_script_dry_run` — only checks log lines, does not verify idempotency (run twice), does not verify no real systemd calls occur
- ❌ No test for `xibi_rollback.sh`
- ❌ Tests assume portable environment but service files have hardcoded `/home/dlebron/` paths (will cause inconsistency once Blocker #2 is fixed)

### Vision Notes

This step is the right one to ship — deploying a working Xibi onto NucBox is high-value and the overall design is sound. The cutover/rollback approach is safe and the config migration is reasonable. The three blockers are fixable and do not require architectural changes:

1. **MCPServerRegistry**: Either defer the full `cmd_heartbeat` body to a later PR (once step-35 is confirmed merged), or import it properly if it already exists.
2. **Hardcoded paths**: Replace `/home/dlebron/` with systemd `%h` specifiers in service files.
3. **DEPLOY.md**: Add the runbook section.

None of these require fundamental redesign.

### Summary

PR #40 implements the NucBox cutover infrastructure (systemd service files, cutover/rollback/deploy scripts, config migration) correctly in most respects. However, it has three hard blockers that prevent merge: (1) `cmd_heartbeat` references `MCPServerRegistry` which is not imported and may not exist in the codebase at this step, causing a fatal runtime error; (2) systemd service files contain hardcoded `/home/dlebron/` paths that violate portability and the spec's idempotency constraint; and (3) `DEPLOY.md` was not updated despite being an explicit required deliverable. A rejection comment has been posted to the PR with specific fix instructions. Do not merge until these are resolved and Opus re-reviews.
