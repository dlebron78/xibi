# Pipeline Review — 2026-04-06 03:53

## Action taken

**Merged PR #56 (Step 52 — Web Search MCP Source) after Opus APPROVE, moved
step-52 to done/, wrote step-53 spec (Filesystem MCP Source), and fired Jules
immediately — session `9557322652980083490`.**

Pre-merge: pushed two CI fix commits to the PR branch:
1. `fix: lint — sort imports, remove unused vars in test_web_search_source.py`
2. `fix: ruff format source_poller.py (format check failure)`

Both were trivial lint/format issues. All 4 CI checks passed after fixes.

Note: First Opus review returned REJECT based on incomplete information (only given the
diff, which starts at line 44 of extractors.py — Opus concluded `SignalExtractorRegistry`
was undefined, but the class is defined at line 10 of the existing file). Re-ran Opus
with complete file contents; second review returned APPROVE.

---

## Pipeline state

- Last merged: Step 52 — Web Search MCP Source: Brave/Tavily Integration (PR #56)
  merged 2026-04-06T03:52 UTC, merge SHA `4ee54991bfd351a9b6bddbfc88441bbec0bd66a7`
- In flight: `step-53-filesystem-mcp-source` — Jules session `9557322652980083490`,
  triggered 2026-04-06T03:53 UTC
- Next up (pending): none — step-53 already promoted to triggered/
- Queued (blocked): none
- Fired Jules: **yes — step-53-filesystem-mcp-source**, session `9557322652980083490`

---

## Pre-Review CI Fixes

CI was failing on 2 checks when the pipeline driver picked up PR #56.

### Fix 1: lint — `tests/test_web_search_source.py` (ruff I001, F401, F841)
Root cause: Jules left `test_web_search_source.py` with unsorted imports,
an unused `SignalExtractorRegistry` import, and two unused local variables
(`skills_dir`, `adapter`) in `test_source_metadata_propagated_to_extractor_context`.
Fix: `ruff check --fix --unsafe-fixes tests/test_web_search_source.py` auto-fixed
all 4 issues. Pushed as `fix: lint — sort imports, remove unused vars in test_web_search_source.py`.

### Fix 2: lint — `xibi/heartbeat/source_poller.py` (ruff format)
Root cause: Jules split a generator expression across two lines in a style that
ruff format wanted combined. `ruff format --check xibi/` caught it.
Fix: `ruff format xibi/heartbeat/source_poller.py`. Pushed as
`fix: ruff format source_poller.py (format check failure)`.

**CI state after fixes:** secrets-check ✅, lint ✅, typecheck ✅, test ✅ (all 4 green).

---

## Opus Review — PR #56 (Step 52)

**Verdict: APPROVE**

*Note: First Opus run REJECTED due to incomplete diff input (class definition was
outside the diff window). Ran a second time with full file contents. All findings below
are from the correct second review.*

### Spec alignment

- ✅ Every requirement in Goal implemented: web_search extractor, _url_to_ref_id(), _extract_domain(), _poll_watch_topics(), source_metadata propagation, watch_topics in profile.example.json
- ✅ All 5 required files present and correctly modified (extractors.py, source_poller.py, poller.py, profile.example.json, tests/test_web_search_source.py)
- ✅ Test count: 16 tests (spec required 12 minimum); all 14 required test cases plus 2 extras
- ✅ Constraints: no hardcoded tool names, watch_topics in profile (not config), no new DB tables, type annotations on all new public functions, source_metadata additive, no existing extractors modified, max_results clamped to [1-10] with warning

### Architecture alignment

- ✅ No model names in code (zero `get_model()` or hardcoded model references)
- ✅ Python collects/routes/enforces; signal extraction is pure deterministic Python (boundary not blurred)
- ✅ No new SQLite state bypassing migration pattern (last_poll is runtime-only dict)
- ✅ Source tagging present: results tagged with `"source": f"web_search:{query[:30]}"`, flow through is_duplicate_signal() dedup
- ✅ MCP tier assignments unchanged (MCPServerRegistry enforces RED tier globally)
- ✅ Asyncio justified: `_poll_watch_topics()` awaits `client.call_tool()` (MCP async call)
- ✅ Web search as signal source: architecture doc explicitly lists Tavily in channel table as "Search | Tavily | Query results | — (read-only)"
- ✅ Beliefs/session_turns not touched

### Test quality

- ✅ Tests assert meaningful behavior: hash stability, collision resistance, domain edge cases, fallback to generic, MCP call timing (due vs skipped), max_results clamping, source_metadata propagation through full async_tick() path
- ✅ All mocked: AsyncMock for MCP client, MagicMock for registry, patch for internal calls; no real HTTP or model calls
- ✅ 16 tests, all passing (locally verified: 133 total test suite, 0 failures)

### Operational readiness

- ✅ No new DB writes; existing open_db() pattern unchanged
- ✅ Exception handling: `exc_info=True` in `_poll_watch_topics()` poll loop
- ✅ No DB write contention (no new writes)
- ✅ Initialization at startup: SourcePoller instantiated once in HeartbeatPoller.__init__
- ✅ Graceful config absence: `self.config.get("watch_topics", [])` returns [] without error
- ✅ Unclean shutdown safe: no new persistent state
- ✅ No new services requiring SIGTERM handling

### Vision notes

Step-52 validates the "new source = config entry" promise from step-48. Web search
results now flow into the observation cycle automatically for any profile with a
`watch_topics` entry and a configured Brave/Tavily MCP server. This directly unblocks
Phase C (chief-of-staff external news surfacing) and Phase E (tourism chatbot context).

Trade-offs accepted for MVP:
- Poll state is ephemeral (lost on restart) — can be persisted in a future step
- No backoff/rate-limiting in Xibi code — delegated to MCP server (appropriate separation)
- Results quality depends on web search provider — unavoidable

### Summary

PR #56 adds a complete, working web search signal pipeline: a dedicated extractor that
converts Brave/Tavily MCP results into structured `web_result` signals with stable
URL-based dedup IDs, a configurable watch-topic poller that checks intervals and clamps
max results, and bidirectional context propagation so the original query is available
inside the extractor. All 16 tests pass, CI is green, and the implementation respects
the architectural boundary between Python (collecting/routing) and the LLM (reasoning).
The code is clean, well-annotated, and ready to ship.

---

## Step 53 Spec Written: Filesystem MCP Source

Written `tasks/triggered/step-53-filesystem-mcp-source.md` (promoted directly to
triggered/ by jules_trigger.sh).

**Spec summary:**
- Adds `file_content` signal extractor with helper functions `_path_to_ref_id()`,
  `_extract_filename()`, and `_extract_extension()` to `extractors.py`
- Adds `_poll_watch_dirs()` method to `source_poller.py`, mirroring `_poll_watch_topics()` pattern
- Adds `watch_dirs` config to `profile.example.json`
- Uses `@modelcontextprotocol/server-filesystem` (official MCP package) with stdio transport
- Scoped sandbox: server restricted to a single directory via CLI arg
- No changes to `poller.py` (source_metadata context propagation already wired by step-52)
- Minimum 12 tests required; all mocked, no real filesystem access
- P1 backlog items (nudge wiring, heartbeat refactor, thread lifecycle) are all stale —
  all were addressed by step-46a, step-51, and step-50 respectively.

**Why this step:** Next in the explicit MCP server sequence from BACKLOG
("Filesystem (scoped sandbox) — validates stdio lifecycle"). Directly enables chief-of-staff
local file context and tourism chatbot document ingestion.

