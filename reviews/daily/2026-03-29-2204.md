# Pipeline Review — 2026-03-29 22:05

## Action taken
Closed step-35a bookkeeping: confirmed all 3 fixes (DEFAULT_TIER=RED, reflex trust wiring,
source column) were merged in PR #38 (Opus APPROVED). Moved step-35a from tasks/triggered/
to tasks/done/. step-36 (Deployment) is in tasks/pending/ — waiting for NucBox cron to fire Jules.

## Pipeline state
- Last merged: Step 35 / Step 35a — MCP Foundation + Safety Remediation (via PR #38)
- In flight: none (tasks/triggered/ empty)
- Queued: step-36.md (NucBox cron pending)

## Step-35a Resolution

**No separate PR was needed.** PR #38 (MCP Foundation) incorporated all three step-35a fixes,
which were confirmed present and correct by the Opus APPROVE review (see 2026-03-29-2112.md):

### Fix 1 — DEFAULT_TIER = RED (xibi/tools.py line 14)
✅ Confirmed: `DEFAULT_TIER = PermissionTier.RED` present in main.
Opus review verified: "DEFAULT_TIER changed GREEN→RED" in spec alignment.

### Fix 2 — Reflex trust wiring (xibi/observation.py)
✅ Confirmed: `_run_reflex_fallback()` now accepts `trust_gradient` parameter (line 525);
`trust_gradient.record_failure("text", "review", FailureType.PERSISTENT)` and
`record_failure("text", "think", FailureType.PERSISTENT)` are called at lines 571–572.
Call site at line 258 passes `self.trust_gradient`. Opus confirmed in test quality section.

### Fix 3 — source column on session_turns (xibi/db/migrations.py + session.py)
✅ Confirmed: Migration 15 adds `source TEXT NOT NULL DEFAULT 'user'` via ALTER TABLE
(migrations.py line 413). `Turn` dataclass has `source: str = "user"` (session.py line 32).
`add_turn()` persists source in INSERT (session.py lines 181, 192). Read path restores it
(session.py line 265). Opus confirmed in spec alignment.

### Tests
✅ `tests/test_safety_remediation.py` exists with 11 tests covering all three fixes,
all passing as part of PR #38's 774-test CI run.

**Verdict: step-35a COMPLETE — moved to done/.**

## PR #39 Note
PR #39 (mcp-foundation-9156793708217061001) was closed by Jules as a duplicate of PR #38,
with confirmation that PR #38 "covers the same work with all CI checks passing and safety
fixes included." No action needed.

## Step-36 (Xibi Deployment — NucBox Cutover)
spec is in tasks/pending/. Content is deployment infrastructure:
- systemd/xibi-telegram.service and xibi-heartbeat.service unit files
- scripts/xibi_cutover.sh (idempotent, --dry-run mode)
- scripts/xibi_rollback.sh (Bregger restore)
- scripts/xibi_config_migrate.sh + .py (Bregger→Xibi config migration)
- DEPLOY.md update with Xibi runbook
- Test: dry-run exit code 0, config migrate produces valid schema

This is the go-live step. NucBox cron will fire Jules; waiting for PR to open.
