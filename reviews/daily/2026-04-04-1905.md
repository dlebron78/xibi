# Pipeline Review — 2026-04-04 19:05

## Action taken
CI failing on PR #52 (step-48). Fixed 4 ruff lint errors (I001 import ordering) directly and pushed to PR branch. Posted detailed correctness-failure comment to PR for Jules — 60+ test failures and 22 mypy errors all stem from async propagation being incomplete. Cannot proceed to Opus review until CI is green. No merge this run.

## Pipeline state
- Last merged: Step 47 — MCP Semantic Alignment (step-47-mcp-semantic-alignment.md in done/)
- In flight: PR #52 — Multi-Source Framework Implementation (step-48-multi-source-framework)
- Next up: none (tasks/pending/ is empty)
- Queued (blocked): none (tasks/queued/ is empty)
- Fired Jules: no — CI still failing, waiting for Jules to fix correctness issues

## CI Status — PR #52 (Step 48, SHA: 69b5fce → 27778d7 after lint fix)

**Lint:** ✅ Fixed — 4 I001 import-ordering errors in poller.py, skills/__init__.py, nudge.py, sample nudge tool. All fixed with `ruff --fix` and pushed directly.

**Typecheck:** ❌ FAILING — 22 errors across 5 files.

**Tests:** ❌ FAILING — 60+ test failures across ~15 test files.

## Root Cause Analysis

Single root cause for both typecheck and test failures: **async propagation incomplete**.

Two functions were changed from sync to async:
- `xibi/react.py`: `def run(...)` → `async def run(...)`
- `xibi/observation.py`: `ObservationCycle.def run(...)` → `async def run(...)`

`xibi/channels/telegram.py` was correctly updated with `asyncio.run(react_run(...))`. But:
- `xibi/cli/chat.py` line 316: still calls `run(...)` synchronously → coroutine never awaited → all downstream attribute accesses fail with `AttributeError: 'coroutine' object has no attribute 'answer'`
- **All pre-existing test files** still call both functions synchronously — same error pattern

Secondary typecheck issues:
- `xibi/heartbeat/extractors.py` lines 16-17: missing return type annotations
- `xibi/heartbeat/source_poller.py` lines 73, 78: `Returning Any` from typed return
- `xibi/mcp/client.py` line 123: `Returning Any` from typed return

## Failing test categories
- `test_react.py`: all `test_run_*` tests — `'coroutine' has no attribute 'exit_reason'`
- `test_observation.py`: all `test_run_*` tests — `'coroutine' has no attribute 'ran'`
- `test_cli.py`: all integration tests — `'coroutine' has no attribute 'answer'`
- `test_control_plane.py`, `test_native_tool_calling.py`, `test_resilience.py`, `test_react_routing.py`, `test_shadow.py`, `test_tracing.py`, `test_tracing_step41.py`, `test_trust_integration.py`, `test_poller.py`, `test_safety_remediation.py`, `test_schema_validation.py`, `test_operational_hardening_remaining.py`: same pattern

## New test files (from this PR)
`tests/test_source_poller.py`, `tests/test_extractors.py`, `tests/test_nudge.py` — could not verify pass/fail status in isolation (CI ran all tests together). Their failures may be masked by or secondary to the async issue.

## Comment posted to PR
PR comment (id: 4187892289) posted at https://github.com/dlebron78/xibi/pull/52#issuecomment-4187892289 with:
- Exact file + line fix for `cli/chat.py` (wrap in `asyncio.run()`)
- Full list of 15 affected test files
- Two implementation options (asyncio.run vs pytest-asyncio)
- Fix instructions for the 3 minor mypy type annotation issues

## Merge decision
**DO NOT MERGE.** CI failing. Awaiting Jules to fix the async propagation issue across cli/chat.py and all affected test files.

## Next steps
1. Jules reads the PR comment and pushes fixes
2. CI re-runs — must be green on all 4 checks (secrets-check, typecheck, lint, test)
3. Next pipeline run performs Opus review
4. If Opus approves → merge → fire next task
