# Pipeline Review — 2026-03-27 03:28

## Action taken
Merged PR #14: Tier 2: Security Hardening & Honest Health Checks (step-13)

## Pipeline state
- Last merged: Step 13 — Tier 2: Security Hardening & Honest Health Checks
- In flight: step-11b (no PR yet), step-12 (no PR yet) in tasks/triggered/
- Queued: step-14, step-15, step-16 in tasks/pending/

## PR Review: #14

**Spec compliance:** Full. All three fixes from step-13.md implemented:

1. **Path traversal fix** — `_safe_filename()` in `xibi/channels/telegram.py` strips path separators, leading dots, and non-safe chars. Prefixes with `secrets.token_hex(8)`. Upload dir changed from `/tmp/xibi_uploads` (world-readable) to `~/.xibi/uploads` with `mode=0o700`. ✅

2. **Logged deny** — `_is_authorized()` replaces empty-allowlist open access with strict deny-all. Empty `XIBI_TELEGRAM_ALLOWED_CHAT_IDS` now denies everyone and logs a warning. Unauthorized attempts logged to `access_log` table. ✅

3. **Multi-component health check** — `get_system_health()` in `dashboard/app.py` checks DB connectivity, schema version, LLM provider availability, skill registry, and system resources. `/health` endpoint returns JSON. Migration 5 adds `access_log` table; SCHEMA_VERSION bumped to 5. ✅

**Tests:** Comprehensive. Path traversal, hidden file, empty allowlist, access logging, health degraded/healthy cases all covered.

**CI:** All 4 checks passed (typecheck, secrets-check, test, lint).

**Architecture note:** `get_system_health()` fallback for `skills_dir` is slightly fragile (falls back to repo root when `db_path.parent.parent / "skills"` doesn't exist), but acceptable for now — this is a dashboard display function, not a critical path.

## Notes
- step-11b and step-12 remain in triggered/ with no open PRs — Jules has not yet produced branches for these. Will wait.
- steps 14–16 in pending/ are queued for NucBox to fire Jules.
