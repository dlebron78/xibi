# Pipeline Review — 2026-03-28 16:15

## Action taken

**Merged PR #23 (BUG-006/007/008) + PR #26 (step-23). Queued step-24.**

1. **PR #23 conflict resolution** — branch was `dirty` (merge conflict) due to stale metadata and step-22's Telegram per-user isolation having diverged. Rebased onto main: kept step-22's per-user `_active_chats` nudge state, applied PR #23's constructor refactor (`core: Any` → `config/skill_registry`), BUG-006/007 react.py fixes, and `_handle_text` rename. All CI passed after force-push. Merged.

2. **PR #26 (step-23) merge** — all CI green, `mergeable_state: clean`. Implementation reviewed and matched spec exactly. Merged.

3. **step-23 moved to done/** — confirmed after merge API returned `"merged": true`.

4. **step-24 queued** — `tasks/pending/step-24.md` written: Quality-to-Trust Feedback Loop (wire `quality_score_span` → `TrustGradient`, closes observability loop from BACKLOG).

5. **BUGS_AND_ISSUES.md updated** — BUG-006/007/008 marked Fixed.

## Pipeline state

- Last merged: PR #26 — LLM-as-Judge Quality Scorer (step-23), 2026-03-28T16:13Z
- Also merged this run: PR #23 — BUG-006/007/008 fixes, 2026-03-28T16:07Z
- In flight: none (`tasks/triggered/` empty)
- Queued: step-24 (Quality-to-Trust Feedback Loop) in `tasks/pending/`
- Open PRs: none

## Steps completed to date

Steps 01–23 done. BUG-006/007/008 fixed. Quality scoring wired into CLI.

## Notes

### PR #23 complexity

PR #23 was branched before step-22 (per-user Telegram session isolation) merged. The branch introduced `_active_chat_id: int | None` (single-slot state) to replace `_active_chats: dict` — which was the exact opposite of what step-22 added. Resolution: kept step-22's per-user `_active_chats` dict (correct for multi-user), applied PR #23's BUG-008 constructor fix (`react_run()` directly, `config/skill_registry` params) on top. This is the right combination: per-user safety from step-22 + direct-API usage from PR #23.

### step-24 rationale

`quality_score_span` now stores composite scores in spans but doesn't use them to influence routing. The BACKLOG explicitly notes: "persistent quality decline → demote audit interval faster." step-24 closes this loop via `apply_quality_to_trust()` with a neutral zone (2.5–3.5) to prevent noise from average responses biasing the gradient. Light TRANSIENT-style decay for quality failures (not PERSISTENT, which is too aggressive for LLM judge scores).

### CI health

All CI passing on main after both merges. No lint issues. Ruff + mypy clean.
