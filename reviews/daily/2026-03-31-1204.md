# Pipeline Review — 2026-03-31 (automated run)

## Action taken
Waited — pipeline is blocked on Daniel's step-40 spec. No open PRs, no triggered specs. Only
`tasks/pending/step-40-note.txt` is present, which is a hold note authored by Daniel, not a
real spec. The pipeline cannot proceed to step-42 without step-40 being merged first.

Two post-step-41 hotfixes from Daniel were observed directly on main:
- `5467a68` — fix: signal intelligence passing profile dict instead of config to get_model
- `4c3733c` — fix: increase SQLite busy_timeout and add TimeoutStopSec to service files

Both are Daniel-authored direct pushes, not PRs — no pipeline action required.

## Pipeline state
- Last merged: Step 41 — LLM Tracing & Inference Event Recording (PR #42, APPROVE, 2026-03-31)
- In flight: none
- Queued: none (`tasks/pending/step-40-note.txt` is a hold note, not a real spec)

## Merge conflict check
No open PRs → no dirty states to resolve.

## Opus Review
No PR open → no Opus review conducted this run.

---

## Pipeline assessment

### Current state summary
- `tasks/done/`: steps 1–39, 41 (step-40 intentionally absent — held by Daniel)
- `tasks/triggered/`: empty
- `tasks/pending/`: only `step-40-note.txt` — "Held by Daniel. To be queued after step-41 merge."
- Open PRs: **0**
- Recent direct commits on main: 2 hotfixes (5467a68, 4c3733c) from Daniel — no action needed

### Decision: Wait

Step-40 note explicitly says: "Held by Daniel. To be queued after step-41 merge."
Step-41 was merged as PR #42 on 2026-03-31. The gating condition is satisfied.
However, per pipeline constraints, the pipeline driver does not auto-author step-40 — Daniel
must write the onboarding spec himself. Step-42 (MCP Phase 2) is blocked until step-40 is done.

---

## Flags for Daniel

### 🟡 Step-40 is still waiting for your spec

The gating condition for step-40 is met (step-41 merged 2026-03-31). The pipeline is at
rest and will remain so until you write and push the step-40 (Onboarding) spec to
`tasks/pending/`. Once pushed, NucBox will trigger Jules automatically within ~30 minutes.

### ℹ️ Two operational hotfixes landed since last review

Both were applied by Daniel directly to main — no pipeline action required:

1. **signal intelligence config bug** (`5467a68`) — `poller.py` was passing `self.profile`
   (a user profile dict) as the `config` argument to `enrich_signals`, causing `KeyError('models')`
   on every signal intelligence tick. Fixed by threading `config_path` through
   `enrich_signals` and `extract_tier1_batch`.

2. **SQLite busy_timeout + service shutdown** (`4c3733c`) — `open_db()` had a 5s busy_timeout
   causing frequent "database is locked" errors under concurrent write load (telegram + heartbeat).
   Raised to 30s. `xibi/db/migrations.py` was using raw `sqlite3.connect()` with no timeout at
   all — now also uses `open_db()`. Service files gain `TimeoutStopSec=120` so a 60s in-flight
   Ollama call doesn't trigger premature SIGKILL on shutdown.

### ℹ️ Step-42 spec is ready in git history

Once step-40 is cleared, the step-42 spec (MCP Phase 2: Resources, Resilience & Xibi-as-Server)
is preserved in git history at commit `7dc2650`. It can be immediately restored to
`tasks/pending/` after step-40 merges.

---

## Summary
Pipeline is healthy and at rest after step-41 (LLM Tracing) merged cleanly. Two operational
hotfixes from Daniel have been applied directly to main, improving SQLite reliability and fixing
a signal intelligence config bug. The pipeline driver cannot proceed until Daniel writes the
step-40 (Onboarding) spec — this is a Daniel-authored task by design.
