# Pipeline Review — 2026-04-06 (automated)

## Action taken
Rejected PR #58 (step-54) after Opus code review — spec constraint violations. No merge. Review comment posted to PR. Pipeline holds at step-53 merged. Jules must revert out-of-scope changes before re-review.

## Pipeline state
- Last merged: Step 53 — Filesystem MCP Source
- In flight: PR #58 (step-54) — REJECTED, awaiting fixes from Jules
- Next up: step-54 (blocked on PR #58 fix)
- Queued (blocked): none
- Fired Jules: no — PR #58 rejection must be resolved first; pipeline gate holds

## Additional open PR
- PR #59 (branch: architecture-alignment-update) — "Vision alignment: reframe identity, autonomy-trust matrix, new gates" — not a pipeline step spec. CI failing (test + lint). Not reviewed this run; not a step-54 or later dependency. Flagged for Daniel to handle separately.

---

## Opus Review — PR #58 (Step 54: GitHub MCP Source)

**Verdict: REJECT**

### Spec alignment

✅ Goal section implemented — `github_activity` extractor added, GITHUB_TOKEN guard in `_poll_watch_repos()`, no new DB tables, `watch_repos` config in profile  
✅ `tests/test_github_source.py` created — 18 tests, all spec-named cases present  
✅ `profile.example.json` updated with `watch_repos` block  
✅ `xibi/heartbeat/extractors.py` updated — `_sha_to_ref_id()`, `_issue_to_ref_id()`, `extract_github_activity_signals()` all present, module-level  
✅ `xibi/heartbeat/source_poller.py` updated — `_poll_watch_repos()` + `poll_due_sources()` integration correct  
❌ **`xibi/heartbeat/poller.py` was modified** — spec says "No changes to `xibi/heartbeat/poller.py`". Three changes found:
  - `digest_tick()`: Added `if force: self._broadcast("No new digest items.")` (new behavior, not in spec)
  - Quote style normalization: `'verdict'` → `"verdict"` (cosmetic but still a change)
  - Removed `ticks_per_hour = max(1, 60 // self.interval_minutes)` from `run()`
❌ **Out-of-spec file modifications**: `xibi/cli/skill_test.py` (input_schema → inputSchema) and `skills/nudge/manifest.json` (same rename + JSON reformatting) — neither file is in the step-54 spec. May be valid fixes but need their own scoped PR.

### Architecture alignment

✅ No model name hardcoding — no references to specific model strings anywhere in diff  
✅ Python collects/routes/enforces; roles reason — `_poll_watch_repos()` gathers and routes, LLM reasoning downstream  
✅ No new SQLite state — `_poll_watch_repos()` returns dicts, no DB writes  
✅ Channel/tool/skill/transport terminology correct — "signal", "tool", "source", "extractor" all used correctly  
✅ MCP tools: `default_tier: "RED"` visible in spec's example config; implementation defers to config  
✅ `async` introduced in `_poll_watch_repos()` is justified — MCP `call_tool()` is async  
✅ Beliefs/session_turns untouched — correct for this step  

### Test quality

✅ 18 tests (exceeds minimum 14), all spec-named test cases present  
✅ Meaningful assertions — ref_id values verified, skip logic verified, interval enforcement verified  
✅ Failure paths covered — no token, no server, skip when not due, missing required fields, max_items out of range  
✅ All mocked — `AsyncMock` for `call_tool`, `patch.dict(os.environ)` for GITHUB_TOKEN, no real network calls  

### Operational readiness

✅ No DB layer touched — no open_db() concern  
✅ Exception handling: `logger.error(..., exc_info=True)` on per-repo per-event-type try/except — correct  
✅ No multi-process DB contention — this step adds no DB writes  
✅ No hot-path initialization  
✅ GITHUB_TOKEN validated at `_poll_watch_repos()` entry (before any calls), not mid-request  
✅ No in-flight DB state to corrupt on shutdown  
✅ No new long-running process — N/A  

### Vision notes

The step is directionally correct and technically solid. It adds GitHub repo watching as a read-only signal source with a clean MCP integration, proper dedup strategy, and independent per-event-type polling. This is the right next step toward a chief-of-staff agent that watches Daniel's work streams.

The out-of-scope changes are likely genuine improvements (`digest_tick` broadcasting "No new digest items" on force is arguably better UX, `ticks_per_hour` was unused), but bundling them violates the one-concern-per-PR discipline. This matters: it makes it harder to bisect failures and makes the review gate meaningful.

The `inputSchema` vs `input_schema` fix in `skill_test.py` and `nudge/manifest.json` is likely a real bug — MCP spec uses `inputSchema`. That should be its own micro-PR or part of a cleanup step.

### Summary

PR #58 implements step-54 correctly in all substantive ways: extractor logic, polling integration, dedup helpers, test coverage, and architecture compliance are all sound. The rejection is on scope grounds only — changes to `poller.py` and two files outside the spec's stated scope. Jules needs to either revert these to a separate PR or get an explicit spec amendment. Once that's done, this is an APPROVE.

---

## Next run guidance

- Do not re-review PR #58 until Jules has addressed the REJECT comment
- If > 2 hours pass with no follow-up commit on PR #58, check Jules session status for stall
- PR #59 (architecture-alignment) is not a pipeline step — leave for Daniel to handle
- Pipeline gate: `tasks/triggered/` still has step-54 spec; do not fire next until step-54 merges
