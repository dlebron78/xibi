# Pipeline Review — 2026-03-27 06:34

## Action taken

Implemented remaining step-12 fixes (Fix 3 + Fix 5) directly to main — Jules has not
picked up the triggered spec in >24 h. All 534 tests pass. Moved step-12.md to done/.

**Changes committed:**

- `xibi/alerting/rules.py` — Added `pop_digest_items()`: atomically reads triage items
  and advances the watermark in a single SQLite transaction. Prevents duplicate digest
  delivery when two heartbeat processes race.

- `xibi/heartbeat/poller.py` — Updated `digest_tick()` to call `pop_digest_items()`
  instead of `get_digest_items()` + `update_watermark()`. The watermark is now advanced
  inside the same SQLite transaction that reads the items.

- `xibi/channels/telegram.py` — Added `_is_already_processed()`, `_mark_processed()`,
  `_purge_old_processed_messages()`. Updated `poll()` to check and mark message_id in
  the `processed_messages` table before/after handling each message. Eliminates
  skip-on-crash and duplicate-on-crash failure modes.

- `xibi/db/migrations.py` — Added migration 6: `processed_messages` table
  (message_id INTEGER PRIMARY KEY, processed_at DATETIME). Bumped SCHEMA_VERSION to 6.

- `tests/test_poller.py` — Updated mock-based tests for the `pop_digest_items` rename.
  Added `test_watermark_race_condition_safe` (concurrent ticks, threading) and
  `test_digest_tick_uses_pop_digest_items`.

- `tests/test_telegram.py` — Added 5 idempotency tests: `_is_already_processed`,
  `_mark_processed`, idempotency of double-mark, poll skips pre-marked message,
  TTL purge test.

## Pipeline state

- Last completed: Step 12 — Tier 1 Critical Bug Fixes (all 5 fixes now merged to main)
- In flight: None (no open PRs)
- Triggered (stale): step-11b.md (~30h+ no Jules branch)
- Pending: step-15.md, step-16.md (NucBox has not fired — stall now >21h)

## Notes

**step-11b (Trust Gradient Hardening):** Still in tasks/triggered/ with no Jules branch.
The three changes (probabilistic audit, failure classification, model-hash auto-reset) are
non-trivial (14 tests). Leaving for a future run. Will implement directly if NucBox
continues to stall.

**NucBox stall continues:** step-15 and step-16 have been in pending/ for >21 hours.
NucBox should fire them on its next cron run now that the daily cap has reset for March 27.
If NucBox is offline, Daniel needs to check NucBox cron and ~/.jules_trigger_state/trigger.log.
The scripts/jules_trigger.sh --check-pending command can be run manually to fire step-15.

**CI health:** All 534 tests pass locally. Recent GitHub Actions runs on main are all green.

**Step-12 completion note:** Previous Cowork run (commit 32a37e8) already fixed:
- Fix 1: consecutive_errors reset in react.py ✓
- Fix 2: router health check timeout ✓  
- Fix 4: WAL mode on all SQLite connections (open_db helper) ✓

This run completes:
- Fix 3: Watermark race condition (pop_digest_items atomic transaction) ✓
- Fix 5: Telegram message idempotency (processed_messages table + message_id gate) ✓

All step-12 work is now in main. Spec moved to tasks/done/.
