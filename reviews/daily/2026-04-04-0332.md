# Pipeline Review — 2026-04-04 03:32 (run ~07:30–08:00 UTC)

## Action taken

1. Fixed ruff format CI failure on PR #49 (Jules' fix commit had 3 unformatted files) — pushed `d8ca4dd`
2. Posted Opus REJECT on PR #50 (step-46a: missing tests, duplicate manifest schema) — posted comment #4186674287
3. Waited for PR #49 CI to go fully green (lint/typecheck/test/coverage all pass)
4. Launched Opus code review for PR #49 (step-46: Centralized Entity & Contact System)
5. **Opus verdict: APPROVE WITH NOTES** — all hard requirements met
6. Merged PR #49 → main (SHA: f367f62)
7. Moved step-46 from triggered/ to done/
8. Removed stale step-45 artifact from triggered/
9. Also launched Opus review for PR #50 (step-46a) after CI went green — Opus REJECT

## Pipeline state

- Last merged: Step 46 — Centralized Entity & Contact System (PR #49)
- In flight: PR #50 (step-46a — REJECTED by Opus, Jules must fix tests + duplicate manifest schema)
- Next up: step-46b-patch-dashboard-model-config.md, step-47-mcp-semantic-alignment.md (both in pending/, gated behind triggered/)
- Queued (blocked): step-48-multi-source-framework.md (depends on step-47, not yet done)
- Fired Jules: NO — triggered/ is non-empty (step-46a still awaiting Jules fix)

---

## Opus Review — PR #49 (Step 46: Centralized Entity & Contact System)

**Verdict: APPROVE WITH NOTES**

### Spec alignment

✅ Migration 18 implemented: 7 new columns on contacts, contact_channels table with UNIQUE(channel_type, handle), session_entities.contact_id column  
✅ resolve_contact() implemented with 4-level matching: exact channel → cross-channel domain → name+org → name-only  
✅ Ambiguous match correctly returns None (tested by test_resolve_ambiguous_no_match)  
✅ All matches update last_seen  
✅ upsert_contact() uses resolver + domain inference (colleague/org_known/unknown)  
✅ bridge_to_contacts() outer try/except with logger.warning(exc_info=True) per spec  
✅ 2-session threshold for partial contact creation implemented and tested  
✅ _track_outbound() uses open_db() — no bare sqlite3.connect()  
✅ reply_email.py passes _workdir so send_smtp can call _track_outbound  
✅ get_context_block() enriches person entities with linked contact context  
✅ All 7 files in spec's file structure exist and are modified  
✅ 14 tests: 6 (resolver) + 4 (enrichment) + 4 (bridge) = 14 minimum met  

### Architecture alignment

✅ No model name references — get_model(specialty, effort) used  
✅ Python collects/routes/enforces; all inference is deterministic Python (no LLM in contact path)  
✅ Migration 18 properly registered in SchemaManager chain  
✅ Terminology consistent with architecture doc  
✅ No asyncio added  
✅ session_entities.contact_id is metadata enrichment, not beliefs — no source tagging required  

### Test quality

✅ Tests assert meaningful behavior (relationship inference, ambiguity handling, partial creation threshold, context block format)  
✅ Failure paths covered: ambiguous match, one-off mention no-pollution, missing contact  
✅ No real model calls — all tests use synthetic DB records  
✅ 14 tests (spec minimum)  

### Operational readiness

✅ All write paths: open_db() confirmed for resolve_contact, bridge_to_contacts, create_contact, upsert_contact_channel, upsert_contact, _track_outbound  
✅ bridge_to_contacts outer exception with exc_info=True  
⚠️ Inner exception (partial contact creation) logs with logger.error() without exc_info=True — acceptable (outer catches it, partial creation is secondary)  
✅ _track_outbound silent failures acceptable for fire-and-forget telemetry  
✅ No new background threads or processes  
⚠️ get_context_block() opens one DB connection per person entity per turn — correct, but could batch in follow-up if latency becomes noticeable  
✅ Dashboard endpoint removals (/api/config, /api/traces) have no external consumers detected  

### Vision notes

Core design is sound. Centralized contact model unifies fragmented identity data. Domain inference reduces friction. 2-session threshold prevents contact pollution. Context enrichment surfaces relationship history to the LLM. Solid foundation for the chief-of-staff use case.

**Two follow-up items for Daniel:**
1. Consider batching contact lookups in get_context_block() when entity list grows large
2. Add exc_info=True to inner logger.error in bridge_to_contacts partial creation path (minor consistency fix)

### Summary

Step-46 implements the full centralized entity/contact data layer. Resolver with 4-level matching, domain-based relationship inference, session entity bridging with partial contact creation, and context block enrichment all work correctly. All write paths use open_db(). 14 tests pass. Architecture is clean — Python handles deterministic routing, no LLM in the contact path. This is exactly what a chief-of-staff agent needs to remember people across channels.

---

## Opus Review — PR #50 (Step-46a: Wire nudge() into Executor)

**Verdict: REJECT**

### Reason

PR delivers zero tests for the nudge tool implementation. Spec requires minimum 3 tests (2 unit + 1 integration mock). This is a hard block.

### What must change

1. **Create tests/test_nudge_skill.py** with:
   - Unit test: nudge formats message correctly with emoji prefix and thread reference
   - Unit test: nudge returns error on missing message
   - Integration test: mock TelegramAdapter, verify send_message called
2. **Remove duplicate `inputSchema` field** from manifest.json (keep only `input_schema`)

### What's correct

- Manifest structure sound (tier: YELLOW, access: operator, required fields present)
- nudge.py implementation logic correct
- Fallback path (urllib.request) acceptable since executor doesn't inject context
- CI is green on Jules' latest commit (6a18bea) — lint ✅ typecheck ✅ secrets ✅ test ✅

### CI fix applied this run

Previous commit d5650fb had typecheck failure (mypy: Returning Any from _load_config). Jules fixed with 6a18bea (cast + import + route rename). CI now fully green on PR #50.

---

## Next run priorities

1. Wait for Jules to push fix to PR #50 (tests + duplicate schema removal)
2. Once CI green on fix commit, run Opus review on PR #50
3. If Opus approves PR #50, merge and fire Jules for step-46b
4. Pipeline gate: do not fire while step-46a is in triggered/
