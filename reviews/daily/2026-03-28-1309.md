# Pipeline Review — 2026-03-28 13:09 UTC

## Action taken

**Merged PR #22 + queued step-21 spec.**

1. PR #22 ("CLI Enhancements: History, Debugging, and Tracing") — all 4 CI checks passing (lint, typecheck, secrets-check, test). Mergeable state: clean. Implementation matches step-20 spec exactly. Merged via GitHub API (merge commit SHA: 631b9e7).

2. Moved `tasks/triggered/step-20.md` → `tasks/done/step-20.md`.

3. All pending/ and triggered/ directories now empty. Wrote and pushed `tasks/pending/step-21.md` (LLM Routing Fallback Classifier).

## Pipeline state

- Last merged: PR #22 — CLI Enhancements: History, Debugging, and Tracing (step-20), 2026-03-28
- In flight: none (tasks/triggered/ empty)
- Queued: step-21 (tasks/pending/step-21.md — LLM Routing Fallback Classifier)
- Open PRs: none (PR #22 just merged)
- CI on main: ✅ all checks passing

## PR #22 Review Notes

**step_callback debug output** — correctly replaces raw JSON blobs with structured `[N] toolname(input)` / `thought:` / `← output` format. Matches spec.

**readline history** — wrapped in `try/except ImportError` for Windows, persists to `~/.xibi/cli_history`, 500-entry limit. Correct.

**Thinking spinner** — uses `threading.Thread(daemon=True)` on stderr. Suppressed when `--no-spinner` or `not sys.stderr.isatty()`. Clean.

**Footer** — `(via:react, 42ms, exit:finish, trace:abc123)` format. Correct.

**`/traces` + `/trace <id>` slash commands** — lazy Tracer import, correct.

**Tests** — 4 new tests covering all spec requirements. Good.

Minor deviation from spec: `_thinking` event and `_spin` function defined at module level (spec suggested inside `main()`). This is a valid improvement — allows reuse and avoids re-creating the Event on each call. Not a concern.

## step-21 spec summary

**LLM Routing Fallback Classifier** — activates when BM25 shadow returns None (score < 0.65). Makes a single `generate_structured` call via `get_model("text", "fast")` with compact skill+tool list prompt. Returns `LLMRoutingDecision` only if confidence >= 0.75 AND tool exists in manifests (hallucination guard). Failure modes all return `None` silently. Integration: new `routed_via="llm-hint"` label in CLI footer, `[llm:hint] skill/tool` debug print. Does not modify ShadowMatcher, react.run(), or the BM25 thresholds.

## Notes

- Steps 01–20 all merged and in `tasks/done/`.
- Two P1 backlog items remain: (1) semantic routing fallback — now step-21; (2) per-user Telegram session isolation — will be step-22.
- No architecture blockers. CI is clean.
