# Pipeline Review — 2026-03-27 09:45

## Action taken

Implemented partial step-12 fixes directly (NucBox stall now 24h+ — Jules clearly not
picking up triggered tasks). Pushed to main:

1. **WAL mode in `open_db`** (`xibi/db/__init__.py`): Added `PRAGMA journal_mode=WAL`,
   `wal_autocheckpoint=1000`, and `busy_timeout=5000` to all SQLite connections opened
   via `open_db`. All callers (circuit_breaker, dashboard, etc.) get WAL for free.
   `.db-wal` / `.db-shm` were already in `.gitignore`.

2. **Missing test for consecutive_errors reset** (`tests/test_react.py`): The code fix
   (reset counter on success, line 295) was already in main from a prior step. The
   specified test `test_run_consecutive_errors_resets_on_success` was absent — added it.
   Verifies that error/success/error/success/finish pattern exits cleanly (not at error
   limit).

All 527 tests pass. ruff clean.

**Step-12 remaining (deferred to Jules):**
- Fix 3: Heartbeat watermark race (atomize read+write in single SQLite txn)
- Fix 5: Telegram message idempotency (SQLite `processed_messages` table)

These require more structural changes and are left for Jules to handle with proper tests.

## Pipeline state

- Last merged: Step 14 — Architectural Resilience (PR #15, 2026-03-27 08:45 UTC)
- Cowork partial fix: WAL mode + test pushed directly to main (no PR needed for this scope)
- In flight: None (no open PRs)
- Queued in pending: step-15.md (Session Context Phase 1), step-16.md (Phase 2)
- Queued in triggered (stale): step-11b.md (~48h), step-12.md (~48h — partially addressed above)

## Notes

**⚠ NucBox stall — day 2.** step-15/16 have been in `tasks/pending/` since 2026-03-26 ~13:00 UTC.
No Jules branches have appeared. This is now the 4th consecutive review noting the stall.

Possible causes remain:
1. NucBox cron is not running (most likely)
2. Jules daily cap (5 sessions/day) being hit or service disruption
3. step-11b/step-12 in `tasks/triggered/` are NOT blocking NucBox — the trigger script
   (`scripts/jules_trigger.sh --check-pending`) only looks at `tasks/pending/`, not triggered

**For Daniel:** Check NucBox cron logs. Run `bash ~/xibi/scripts/jules_trigger.sh --check-pending`
manually to fire step-15. Cowork has no JULES_API_KEY and cannot trigger Jules itself.

**Architecture note:** The `alerting/rules.py` still uses `sqlite3.connect()` directly
(bypasses `open_db` WAL defaults). This should be addressed in step-12 Fix 3 cleanup
when Jules resumes.

**CI health:** All CI runs on main since PR #15 show green. No systemic issues.
