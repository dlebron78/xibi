# Pipeline Review — 2026-04-03 22:20

## Action taken
**Merged PR #47** (Native Function Calling in ReAct Loop — step-43) after CI fixes, rebase, and Opus APPROVE WITH NOTES verdict. Three CI fix commits pushed to PR branch before merge. Spec moved from `tasks/triggered/step-43.md` to `tasks/done/step-43.md`.

## Pipeline state
- Last merged: Step 43 — Native Function Calling in ReAct Loop (PR #47, sha `116a27e`)
- In flight: none (0 open PRs)
- Queued (triggered/): `step-44.md` — already merged by NucBox at 20:59 on 2026-04-03; not moved to done/ (no verifiable Opus review record — see findings below)
- Queued (pending/): none

---

## CI Fixes Applied (before Opus review)

Three commits were pushed to the PR branch to resolve all CI failures:

### Commit 1: `587ac2c` — ruff format, mypy cast, test_control_plane assertion
- **ruff format**: `tests/test_bregger.py` and `tests/test_memory_skill_db_migration.py` needed blank-line insertion between function definitions
- **mypy no-any-return**: `xibi/router.py:1037` — `return res` (where `res` is `Any`) in `BreakerWrappedClient.generate_with_tools`. Fixed with `cast(dict[str, Any], res)` and added `cast` to typing imports.
- **Test failure**: `test_react_run_with_control_plane_falls_through` asserted `len(result.steps) == 1`, but PR made `finish` a pseudo-tool (not appended to scratchpad). `test_react.py` was already updated by Jules; `test_control_plane.py` was missed. Fixed: changed assertion to `len(result.steps) == 0`.

### Commit 2: `73ce8b6` — ruff format post-rebase
- After rebasing onto current main (step-44 had been merged into main), the merge commit included files from main that were unformatted: `xibi/react.py`, `xibi/channels/telegram.py`, `xibi/command_layer.py`, `xibi/security/content_scan.py`, `tests/test_react_routing.py`. Applied ruff format to all CI-checked files.
- **Why rebase was needed**: CI runs on the `pull/47/merge` ref (PR merged into main). Main had unformatted files from step-44; the merge commit combined PR content with unformatted main content. Force-pushed rebased branch.

All 4 CI checks passed after fixes: lint ✅ typecheck ✅ secrets ✅ test ✅

---

## Opus Code Review — PR #47 (Step 43)

**Verdict: APPROVE WITH NOTES**

### Spec alignment
- ✅ `react_format="native"` branch implemented in `react.run()`
- ✅ `BreakerWrappedClient` proxies `generate_with_tools` and `supports_tool_calling()`
- ✅ Auto-fallback to json when model doesn't support tool calling
- ✅ Config key `react_format` plumbed through Telegram adapter (`str(self.config.get("react_format", "json"))`) and CLI (`xibi/cli/chat.py` with `--format` flag)
- ✅ `finish` and `ask_user` are pseudo-tools: NOT appended to scratchpad
- ✅ 18 tests in `tests/test_native_tool_calling.py`
- ✅ Spec constraint "do not inject model-generated content into scratchpad" respected: `_native_messages` goes to model, `scratchpad` drives pipeline control
- ⚠️ Spec referenced `xibi/__main__.py` but Jules correctly modified `xibi/cli/chat.py` (which is where `cmd_chat` lives; `__main__.py` uses `TelegramAdapter` and doesn't call `react_run` directly). Spec reference was outdated — not a defect.

### Architecture alignment
- ✅ No model name hardcoding; only `get_model(specialty, effort)` used
- ✅ Python boundary preserved: `_native_step` marshals calls, roles reason
- ✅ No new SQLite state, no schema changes
- ✅ No asyncio introduced
- ✅ Source tagging logic unchanged and intact in tool dispatch path
- ✅ Terminology: tool/skill/channel/transport used correctly throughout

### Test quality
- ✅ 18 tests; all assert meaningful behavior
- ✅ Failure paths covered: empty response, exception from `generate_with_tools`, fallback to json, stuck detection, max_steps, malformed tool_calls
- ✅ All mocked at class level (`patch("xibi.router.OllamaClient.generate_with_tools")`) or on LLM instance directly — no real model calls
- ✅ `test_react_run_native_message_history_grows` verifies Ollama wire format (user + assistant + tool)

### Operational readiness
- ✅ No bare `sqlite3.connect()` added
- ✅ No new background threads
- ✅ `_native_tools` built once before loop (not in hot path)
- ✅ `react_format="native"` config key is optional; missing key silently defaults to json
- ✅ No new state that could cause inconsistent shutdown

### Vision notes (from Opus)
1. Schema normalization (`inputSchema` → `parameters`) correct per Ollama API spec; verify on actual models if not already tested on NucBox.
2. Message history growth is normal for native calling; monitor in long sessions against model context limits.
3. Error handling is defensive; malformed `tool_calls` fall through gracefully. Consider structured logging if debugging tool-calling issues in production.

### Summary
This step replaces fragile text-based JSON parsing in the ReAct loop with Ollama's native tool-calling API via `react_format="native"`. The implementation is well-structured: clean separation between the model's message history (`_native_messages`) and the pipeline's control state (`scratchpad`), correct circuit-breaker integration in `BreakerWrappedClient`, and graceful fallback for non-supporting models. All 18 tests pass, CI is green, and the architecture boundary between Python (routing, enforcing) and the LLM (reasoning) is fully respected.

---

## ⚠️ Findings for Daniel

### 1. Step-44 merged by NucBox without verifiable Opus review
**Commit:** `5a0a8ae` — "Merge PR: Step-44 Context-Aware Tier Resolution"
**Merged by:** NucBox at 2026-04-03 20:59 ET (59 minutes after step-44 was triggered)
**Finding:** No Opus code review file exists in `reviews/daily/` covering step-44. The spec (`tasks/triggered/step-44.md`) was never moved to `done/`. This means step-44 was merged without the mandatory Opus gate — a protocol violation.
**Recommendation:** Daniel should run an Opus review on step-44's diff and decide whether to accept retroactively or require a follow-up fix PR. `step-44.md` has been left in `triggered/` pending this decision.

### 2. JULES_API_KEY unavailable in pipeline environment
`/sessions/*/mnt/Project_Ray/.env.review` contains only `GITHUB_TOKEN`. `JULES_API_KEY` is not present. The stall-check script (`scripts/jules_session_status.sh`) requires it. Stall detection and Jules check-in response are non-functional from this environment. If NucBox has the key in `~/.xibi_env`, stall checks should run from there instead.

### 3. Dual-trigger protocol violation (noted by previous review, now resolved)
Previous run flagged that step-43 and step-44 were both in `triggered/` simultaneously. Step-43 has now been merged and moved to `done/`. Step-44 remains in `triggered/` pending Daniel's decision on the Opus review gap.
