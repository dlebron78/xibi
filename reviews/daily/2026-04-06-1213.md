# Pipeline Review — 2026-04-06 12:13

## Action taken
Rejected PR #61 (step-56: Wire nudge() into Executor) — Opus REJECT verdict on spec-discipline grounds (undeclared file change in diff). Comment posted to PR. Pipeline paused pending Jules fix.

## Pipeline state
- Last merged: Step 55 — dashboard-panel-fixes
- In flight: PR #61 (step-56, wire-nudge-into-executor) — REJECTED, awaiting fix
- Next up: none (step-56 must merge before any new spec is written)
- Queued (blocked): none
- Fired Jules: no — PR is under active rejection review, awaiting fix

---

## Opus Review — PR #61 (Step 56)

**Verdict:** REJECT

### Spec alignment

- ✅ `_register_core_skills()` added to `Executor.__init__` — call site and method both present
- ✅ Implementation matches spec's required signature exactly (`try/except`, user-first policy, `logger.warning` on failure, `logger.debug` on registration)
- ✅ `tasks/triggered/step-56-wire-nudge-into-executor.md` present; PR aligns with spec goal
- ✅ All 8 required tests present by name in `tests/test_executor_core.py`
- ✅ No changes to `SkillRegistry.__init__`, `__main__.py`, or skill files
- ✅ No new tables, migrations, or endpoints
- ❌ **Undeclared file change: `xibi/react.py`** — spec declares only `xibi/executor.py` + `tests/test_executor_core.py`. The diff includes a rename of local variable `error` → `error_obj` in react.py (with added type annotation). This was not declared. This is the hard blocker.

### Architecture alignment

- ✅ No model referenced by name — pure Python registry operations
- ✅ Python collects/routes/enforces; roles not involved in this change
- ✅ No new SQLite state — `_register_core_skills()` only mutates `self.registry.skills` dict
- ✅ Terminology consistent: "core skills", "skill_registry", "sample dir" all match architecture doc
- ✅ MCP not involved — N/A
- ✅ No asyncio introduced — all synchronous file/registry operations
- ✅ Beliefs and session_turns not touched

### Test quality

- ✅ 8 tests present (exactly matching spec requirement)
- ✅ Tests assert meaningful behavior: registry membership, path existence, manifest content, user-override preservation
- ✅ Failure path covered: `test_core_registration_does_not_crash_on_missing_sample_dir`
- ✅ Idempotency tested: `test_core_registration_is_idempotent`
- ✅ User skill preservation tested: `test_user_skill_not_overwritten_by_core`
- ✅ No real model calls — tests use temp dirs and mock patches only
- ⚠️ **Opus concern:** `test_nudge_execution_succeeds_with_mocked_telegram` — Opus flagged the patch target `xibi.skills.nudge.nudge` as potentially wrong given `importlib.util.spec_from_file_location` dynamic loading

### Pipeline driver counter-analysis on test mock concern

After direct repo inspection, I believe Opus's mock concern is **incorrect**:

1. `tools/nudge.py` begins with `from xibi.skills.nudge import nudge` — this imports from the package
2. `Executor._execute_inner` loads `tools/nudge.py` via `spec_from_file_location` with a fresh `exec_module()` on every call
3. When `exec_module()` runs the fresh module, `from xibi.skills.nudge import nudge` looks up the attribute on the (already-imported) `xibi.skills.nudge` module object in `sys.modules`
4. `patch("xibi.skills.nudge.nudge")` sets that attribute to the mock before the test runs
5. Therefore the fresh module load DOES get the mock — the patch target is correct

The CI `test: completed / success` confirms this analysis. Opus's rejection on this basis appears incorrect.

### Opus concern: nudge skill existence

Opus stated: "I checked — `xibi/skills/sample/nudge/` does NOT exist in the repo." 

**This is factually wrong.** Direct inspection of the cloned repo confirms:
```
/tmp/xibi-review/xibi/skills/sample/nudge/
  manifest.json          ← present
  tools/nudge.py         ← present
```
The spec explicitly states: "Its skill package (manifest + tools/nudge.py) exists at xibi/skills/sample/nudge/." Opus's rejection reasons #2 (tests 1/2/6 fragile on filesystem assumptions) were entirely based on this incorrect premise and should be disregarded.

### Operational readiness

- ✅ No DB layer touched by this step
- ✅ No background threads added — registration is init-time only
- ✅ No shared-DB cross-process concerns
- ✅ Initialization is one-time (Executor.__init__), not per-request hot path
- ✅ No env vars required — sample dir resolved via package-relative path
- ✅ Never raises — full try/except with logger.warning fallback
- ✅ No new service or long-running process

### Vision notes

The core implementation is architecturally correct and directly fixes the 67+ failed nudge() attempts in the observation cycle. `_register_core_skills()` is the right abstraction (executor owns tool availability, registry stays generic), and the user-first policy is correct.

**Single blocking issue**: Jules bundled a typecheck fix to `xibi/react.py` (variable rename `error` → `error_obj`) that is not declared in the step-56 spec. This is the sole hard blocker. The fix is legitimate but must be either:
1. Removed from this PR (apply directly to main in a separate commit), or
2. Declared in the spec

### Summary

Step-56 correctly implements core skill auto-registration in the Executor. The `_register_core_skills()` method matches the spec signature exactly, the 8 required tests are present and meaningful, and the architecture alignment is clean. The only blocking issue is an undeclared file change: `xibi/react.py` has a variable rename that was not specified in step-56. Per spec discipline, this must be decoupled. Once removed, this PR should be approved immediately — the core implementation is sound.

---

## Notes for Daniel

1. **Opus review had factual errors.** The rejection was stated as 3 reasons; 2 of them were wrong (nudge skill does exist; mock target is correct). The 1 valid reason (undeclared react.py change) is real but minor.

2. **Pipeline is paused** waiting for Jules to fix PR #61 by removing the react.py diff. This should be a 5-minute fix.

3. **The real production fix is one PR away.** Once #61 merges, the observation cycle can actually nudge. After 67+ failed nudge attempts in production, this is high-value.

4. **Consider:** If you want to unblock faster, you could apply the react.py typecheck fix directly to main yourself (it's a local variable rename, zero risk) and have Jules resubmit #61 without it. Or just let Jules re-push the branch.
