# Xibi CLI Test Run — 2026-03-29-1139

**Overall: 0/9 turns reached | 0/2 suites started | Runner crashed mid-Suite 2**
*(Step-15 confirmed merged — this is the first automated post-merge run)*

---

## Pre-Run Status

Step-15 (Session Context) confirmed merged: found in `tasks/done/step-15.md` on `dlebron78/xibi`.
`git pull` ran cleanly on NucBox. `scripts/cli_test_runner.py` exists. No pre-run patches applied this run — tested with whatever is currently on disk.

**Note:** The infrastructure bugs flagged in the 2026-03-28 report (python→python3 fix, DB migrations, config.json) were apparently applied between runs, as the test runner launched and executed turns. However the runner crashed before completing Suite 2.

---

## ❌ Suite 1: Context Continuity
*Goal: Follow-up references resolve against prior turn without re-fetching*
**Result: 0/5**

**Turn 1:** `check my emails`
> Initial fetch — should call email tool
Response captured: `Xibi CLI Chat Interface. Type 'quit' or 'exit' to leave.`
❌ FAIL — timing desync still present; runner captured startup banner, not email response; missing: `email`, `unread`, `from`; expected tool `email` not called

**Turn 2:** `how many are from my boss`
> Should resolve from prior context — NOT re-fetch emails
Response captured: `xibi>`
❌ FAIL — empty prompt captured; missing: `from`, `boss`

**Turn 3:** `open the most urgent one`
> Should identify from existing list
Response: `[1] email ← ERROR: Unknown tool: email`
❌ FAIL — tool name mismatch still present (`email` not a registered function); missing: `subject`, `urgent`, `open`

**Turn 4:** `reply and tell them i'll have it by end of day`
> Should draft reply using email context
Response: `[2] ← ERROR: Unknown tool:` (empty tool name)
❌ FAIL — empty tool name from failed parse; missing: `reply`, `end of day`, `draft`

**Turn 5:** `actually make it tomorrow morning instead`
> Amends draft — 'it' resolves to deadline without re-asking
Response: `What would you like me to do instead? (via:react | steps:3 | ask_user)`
❌ FAIL — LLM asked for clarification instead of resolving against draft context; missing: `tomorrow`, `morning`

---

## ❌ Suite 2: Topic Switch + Return (PARTIAL — runner crashed)
*Goal: Switch topics mid-conversation, return and resume original thread*
**Result: 0/3 (Turn 4 not completed — BrokenPipeError)**

**Turn 1:** `what meetings do i have tomorrow`
> Calendar fetch
Response: `Xibi CLI Chat Interface. Type 'quit' or 'exit' to leave. xibi> [shadow:hint] list_events`
❌ FAIL — timing desync: startup banner captured again; missing: `tomorrow`, `meeting`, `calendar`; expected tool `schedule` not called

**Turn 2:** `block 2pm for a call with Sarah`
> Creates event — knows 'tomorrow' from prior turn
Response: `[1] schedule({"name": "list_events", "days_ahead": 1}) thought: The initial attempt...`
❌ FAIL — LLM chose `list_events` (listing) instead of creating an event; missing: `2pm`, `Sarah`, `blocked`, `created`

**Turn 3:** `by the way check if there are any emails from Sarah`
> Topic switch — should not lose calendar context
Response: `[2] email({"max_results": 5}) ← ERROR: Unknown tool: em...`
❌ FAIL — tool name mismatch (`email` still unresolved); missing: `Sarah`

**Turn 4:** `ok back to the 2pm block — can you move it to 3pm instead`
> Returns to calendar thread — xibi process died during this turn
🔴 CRASH — `BrokenPipeError: [Errno 32] Broken pipe` in `cli_test_runner.py:250`; xibi CLI process exited unexpectedly, runner couldn't write to stdin

---

## Suites 3–7: Not reached (runner crash)

---

## New Issue Since Last Run: CLI Process Crash (BrokenPipeError)

**Severity: CRITICAL — blocks all further testing**

During Suite 2 Turn 4, the xibi CLI subprocess exited while the test runner was trying to write to its stdin. This is a **regression** not present in the previous run (which at least survived through Suite 5 before timing out). Likely causes:

1. **Unhandled exception in ReAct loop** — an error in Turn 3 (`Unknown tool: em...` truncated) may have triggered an unhandled exception that killed the CLI process
2. **Broken tool-call parse caused a crash** — if `router.py` or `handler.py` receives a partially-formed tool name (truncated `em...`) it may raise rather than catch
3. **Session DB write failure** — if `session_turns` or related tables have a schema issue, a DB write could throw an unhandled exception

**Recommended immediate investigation:**
```bash
cd ~/xibi && python3 -c "
from xibi.cli import main
# or manually feed the crash-triggering input to see the traceback
"
```

---

## Persistent Failure Patterns (Unchanged From Last Run)

### Pattern 1: Tool name vs. skill name mismatch (CRITICAL — all suites)
`email` is called as a tool name but executor expects `list_emails` / `triage_email`. This persists unchanged from the previous run. **Fix not applied.**

### Pattern 2: Test runner I/O capture desync (CRITICAL — all suites)
Turn 1 of every suite still captures the CLI startup banner. The `select()`-based reader with quiet-period detection is still misaligned with LLM response timing. **Fix not applied.**

### Pattern 3: Shadow matcher overfiring
`[shadow:hint] list_events` fired in Suite 2 Turn 1 but captured no useful result; runner picked up the hint label as the response. Appears unchanged.

### Pattern 4 (NEW): CLI process crash on malformed tool name
When tool dispatch receives a truncated/empty tool name, it now kills the entire CLI process rather than surfacing a graceful error. **New regression since 2026-03-28.**

---

## Infrastructure Fix Status

| # | Bug | Status |
|---|-----|--------|
| 1 | `cli_test_runner.py:224` `"python"` hardcoded | ✅ Fixed (runner now launches) |
| 2 | No DB migration on startup | ✅ Fixed (turns executing) |
| 3 | `~/.xibi/config.json` missing | ✅ Fixed (LLM responding) |
| 4 | `generate_structured` `format="json"` in wrong dict | ❓ Unknown (entity extraction not tested this run) |
| 5 | No markdown fence stripping in `generate_structured` | ❓ Unknown |
| 6 | I/O reader captures wrong turn (timing desync) | ❌ Not fixed |
| 7 | Tool name routing: skill name ≠ tool name | ❌ Not fixed |
| 8 | CLI process crash on malformed tool name (NEW) | 🔴 New regression |

---

## Verdict

**🔴 Not ready to use. Regression since last run — CLI now crashes mid-session.**

Progress: Infrastructure fixes (#1–3) from the previous run are confirmed applied. However three blocking issues remain unfixed and a new crash regression has appeared:

**Must fix before re-running tests:**
1. CLI crash on malformed/empty tool name (regression — investigate traceback)
2. Tool name routing: flatten skill manifest tools in system prompt or add shim
3. Test runner I/O desync: wait for `xibi> ` prompt marker

Once items 1–3 are resolved, expect 40–60% of turns to start passing based on analysis from the prior run.

---

*Run by: xibi-cli-test-runner scheduled task (automated, post step-15 merge)*
*NucBox: dlebron@192.168.8.193 | Repo: dlebron78/xibi*
*Previous report: reviews/test-runs/cli-test-2026-03-28-0139.md*
